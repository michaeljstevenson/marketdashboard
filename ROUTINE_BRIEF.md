Nightly Routine Brief — michaeljstevenson.co
## ⚠️ DO NOT STOP AFTER ONE PAGE
This is the single most important rule in this file, and it has been missed
before: after finishing a page — including after it's tested, committed,
pushed, and its PR is green and mergeable — **do not stop and do not consider
the run "done."** Immediately move to the next backlog item (or propose a new
one) and keep building. Only stop when the safety valve below is hit
(~85% of the session window) or the idea backlog is genuinely exhausted.
"I built one good page" is never a finishing condition on its own.
Site context
Independent markets-research portfolio site (data-driven research on markets, macro, and investor psychology). Existing sections: Behavioral/Positioning, Equities, Markets/Macro, Rates/Credit, Alternative/Cross-Asset, Thematic. Tone: professional, data-first, "not investment advice."
Nightly task
Build fully polished equities-research pages — not stubs. The backlog below is a source of ideas, not a restriction: use it as a starting point, but feel free to propose and build other equities-related page ideas that fit the site's style, whether or not they're on the list. No cap on how many per night: build as many as can be finished to the "fully polished" standard below within the run. Never sacrifice polish/data depth just to bump the count — a single well-built page beats several half-finished ones.
Model

* Default to Sonnet 5, effort level: low for standard page-building work (scaffolding, layout, wiring up already-clear data pulls).
* Reserve Sonnet 5, effort level: medium for the harder analytical work — designing the statistical methodology, deciding how to structure a non-obvious dataset, or reasoning through what a metric actually means. Switch up only when the task genuinely calls for it, not by default.

Safety valve
Stop the run once it has consumed ~85% of the current 5-hour session window (Pro plan), even if the backlog isn't exhausted and even if fewer than 3 pages are done. Finish the page in progress to a clean stopping point rather than abandoning it mid-file, then wrap up and open the PR with whatever is complete.
Avoiding repeated/redundant work across runs

* Maintain a "Already built" list at the bottom of this brief file (see section below) and update it at the end of every run. Never re-derive or re-attempt a page already on that list.
* In the PR description, log what was skipped and why (e.g. "IPO Pipeline skipped — no free data source found with sufficient history after exhausting Alpha Vantage + web search") so the next run doesn't re-investigate the same dead end. Add skipped items to the "Already attempted, skipped" list below too.

Core objective: analysis first, then presentation
The real value of this site is turning complex equities/markets datasets into something a reader can actually understand — not just displaying numbers. For every page:

1. Analyze first. Do the actual statistical/quantitative work on the data — don't just plot a raw series. Reference [/factor-analysis](https://michaeljstevenson.co/factor-analysis) as the standard: it runs real statistical tests (correlation, Chow tests for structural breaks, robustness checks across different sample splits), and is transparent about methodology choices and even past mistakes made while building it.
2. Then visualize/present. Turn that analysis into a clean, friendly presentation — charts people can read at a glance, plain-language explanations of what the data means and why it matters, not just jargon or a raw chart dump. Depth of analysis matters more than page count — see Safety valve above.

Scope: Equities section only
This routine is restricted to building out the Equities section of the site. Do not build Markets/Macro, Rates/Credit, Alternative/Cross-Asset, or Behavioral/Positioning pages, even if they're listed as "coming soon" elsewhere on the site.
Idea backlog (starting points — not exhaustive, not required in order)

* Earnings Revisions
* Insider Buying/Selling
* Short Interest Tracker
* Buyback Announcements
* IPO Aftermarket Performance
* Dividend Growth Screener
* Small Cap vs. Large Cap Spread
* International vs. US Relative Performance
* Sector Rotation Model
* Analyst Estimate Dispersion
* Earnings Surprise History (beat/miss rates by sector)
* Buyback Yield vs. Dividend Yield
* New Highs/New Lows Ratio
* Equity Risk Premium by Sector
* Index Concentration (top 10 weight vs. history)
* Relative Strength Leaders/Laggards
* IPO Pipeline / Filing Tracker
* Share Count Trends (dilution vs. buybacks)
* Forward P/E vs. Trailing P/E Divergence
* Earnings Growth vs. Price Performance Divergence
* Sector Correlation Matrix
* Small Cap Liquidity/Volume Trends
* Stock Split Tracker
* Spin-off Performance Tracker
* Equity Factor Performance (value/growth/momentum/quality/low-vol)

Data sourcing (required)

1. Use the Alpha Vantage API (key already configured) as the first choice for any data the page needs — equities, FX, commodities, macro indicators, technicals, etc. Check what Alpha Vantage covers before assuming it can't.
2. If Alpha Vantage doesn't have sufficient depth for a given metric (e.g. missing series, insufficient history, no direct dataset), search free public sources online instead — e.g. FRED, BLS, Treasury.gov, World Bank, ECB/ONS/other central bank data portals, CFTC, exchange sites, or other free datasets relevant to the topic.
3. Do not give up and ship a page with placeholder, mocked, or illustrative-only data. If depth is genuinely unavailable after exhausting Alpha Vantage and a real search for free alternatives, scale the page's scope down (fewer charts/metrics) rather than fabricate data — but exhaust the search first.
4. Note the actual data source(s) used for each chart/metric in the page's methodology blurb and in the PR description.

"Fully polished" means

* [ ] Matches existing page layout/style conventions (check an existing page like /sector-analysis or /volatility as the template)
* [ ] Real data wired up (not placeholder text/numbers) where feasible via available APIs
* [ ] Chart(s)/visualization consistent with the site's existing chart style
* [ ] Short methodology/explainer blurb, matching the site's tone
* [ ] Added to the correct nav section, "coming soon" tag removed if applicable
* [ ] Responsive / doesn't break mobile layout

Boundaries — do not touch

* Only build pages under the Equities section (see Scope above) — do not create or modify pages in any other section
* Do not modify any already-live research page (Sentiment Index, Volatility, Sector Performance, etc.) unless explicitly asked
* Do not change deploy config, site-wide nav structure, or global styles
* Do not push to `main` — open a PR against a new branch per run

Output

* One PR per night, branch name `nightly/<date>-<short-topic>`
* PR description: what was built, what data source was used, what was skipped and why, anything that needs a manual decision (e.g. data source choice, ambiguous methodology)

Already built
(Update this list at the end of every run — do not re-attempt these)

* **Earnings Revisions** (2026-09-13) — analyst EPS estimate revision breadth (Net Revision Ratio) and magnitude (Estimate Drift) across the S&P 500, by sector, with a historical trend, breadth-vs-magnitude scatter, and upgrade/downgrade leaderboards. Data: Alpha Vantage `EARNINGS_ESTIMATES` (weekly full-universe sweep), sector/name metadata reused from the Sector Beeswarm page's own weekly refresh. See `netlify/functions/scheduled-revisions-background.js` for full methodology notes.
* **Index Concentration (top 10 weight vs. history)** — already live as `/concentration.html` (built prior to this routine's existence; noting it here so a future run doesn't re-derive it from the backlog).
* **Insider Buying/Selling** (2026-09-13, `nightly/2026-09-13-insider-buying-selling`) — `/insider-buying-selling.html`. Officer/director/10%+-owner Form 4 activity (Alpha Vantage `INSIDER_TRANSACTIONS`) across the full S&P 500, trailing 90 days: net $ buy/sell flow by sector, cluster-buying screen (2+ insiders buying, zero selling), largest individual purchases/sales, full sortable per-stock table. Backend: weekly `scheduled-insider-transactions-background.js` (Saturday 10:30 UTC) → Netlify Blobs (`insider-transactions` store) → `insider-transactions.js`, reusing the Sector Beeswarm page's `meta.json` for company name/sector rather than a second OVERVIEW sweep. See that run's PR description for the full write-up, including the signal-quality filtering rationale (Common-Stock-only, priced-only transactions) and its limits.
* **Small Cap vs. Large Cap Spread** (2026-09-13, `nightly/2026-09-13-smallcap-vs-largecap`) — `/small-cap-vs-large-cap.html`. Relative strength of IWM (small-cap) and MDY (mid-cap) vs. SPY (large-cap), indexed since IWM's May 2000 inception: long-run ratio chart, trailing-return ladder (1M–10Y, cumulative under 1Y / annualized 3Y+), rolling 12-month relative-return oscillator, average monthly spread return by Fed-funds-rate regime (Hiking/Holding/Cutting, classified off trailing 3-month change), and a Pearson+Spearman regression of monthly spread return against the month's change in the 10-year Treasury yield (methodology matches `/factor-analysis`'s two-method check). Backend: daily `scheduled-smallcap-background.js` (weekdays 21:15 UTC — only 5 Alpha Vantage calls, no heavy pacing needed) → Netlify Blobs (`smallcap-largecap` store) → `smallcap-largecap.js`. Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` (IWM/MDY/SPY, full history) + `FEDERAL_FUNDS_RATE` + `TREASURY_YIELD` (10-year, both monthly, full history). Tested locally with a synthetic-but-realistic dataset (real Fed-funds/Treasury history, simulated price paths) served through a local stub API and driven with Playwright (dark/light/mobile) — caught and fixed two bugs pre-merge: floating-point-artifact y-axis tick labels on the regime bar chart, and a non-pluralized "1th percentile" ordinal.
* **Share Count Trends (dilution vs. buybacks)** (2026-09-13, `nightly/2026-09-13-share-count-trends`, stacked on top of the Small Cap vs. Large Cap Spread branch/PR above — merge that one first, or merge this PR as-is since it carries both diffs) — `/share-count-trends.html`. Reads buybacks vs. dilution directly off quarterly `commonStockSharesOutstanding` (Alpha Vantage `BALANCE_SHEET`) at 1/3/5-year lookbacks across the full S&P 500 — deliberately not sourced from separate buyback-announcement or dollar-value data, since the share count itself already nets out every buyback, issuance, and stock-based-comp dilution. Sector aggregates, a year-over-year persistence check (Pearson+Spearman, same non-overlapping-annual construction as `/factor-analysis`'s momentum test — does last year's buyback pace predict this year's?), buyback/dilution streak leaderboards, and a full sortable company table. Backend: weekly `scheduled-share-count-background.js` (Saturday 10:50 UTC, after the insider-transactions sweep), reusing Sector Beeswarm's `meta.json` for company name/sector. Tested locally the same way as this session's other pages (synthetic dataset with a deliberately-correlated year-over-year buyback signal, served through a local stub API, driven with Playwright in dark/light/mobile) — no bugs found.
* **Sector Correlation Matrix** (2026-09-13, `nightly/2026-09-13-dividend-growth`, stacked on top of the Share Count Trends branch/PR above — merge that one first, or merge this PR as-is since it carries both diffs) — `/sector-correlation.html`. Full pairwise Pearson correlation matrix (heatmap) across the 11 SPDR sector ETFs, a rolling 63-day average-correlation time series, and a direct test of the "correlations go to 1 in a crisis" claim (rolling avg. correlation regressed against rolling SPY realized volatility, Pearson+Spearman). Needed zero new Alpha Vantage calls or backend function — it's pure client-side analysis of the daily price history the existing `/api/sector-performance` endpoint (behind Sector Performance) already fetches and serves. Tested locally with a synthetic dataset that includes a deliberate "stress regime" (elevated common-factor weight over one stretch) to confirm the vol/correlation relationship renders correctly (r ≈ +0.98 in the synthetic case, as designed). No bugs found.
* **Sector Rotation Model** (2026-09-13, `nightly/2026-09-13-sector-rotation`, stacked on top of the Sector Correlation Matrix branch/PR above — merge earlier PRs first or merge this one as-is) — `/sector-rotation.html`. A Relative Rotation Graph (RRG)-style view of the 11 SPDR sector ETFs: current level (3-month relative-strength change) vs. momentum (1-month relative-strength change), classified into the standard Leading/Weakening/Lagging/Improving quadrants; a grouped-bar view of relative return across 1M/3M/6M/1Y; and a month-over-month rank-persistence test (Spearman) asking whether chasing last month's sector leaders actually works. Also reuses `/api/sector-performance` with no new Alpha Vantage calls. Tested locally with a synthetic dataset; found and fixed a real bug during testing — a sector label near the RRG chart's right edge (a strong "Leading" sector, the most interesting case) drew off-canvas on narrow viewports, now flips to the point's left when there isn't room on the right.
* Considered but skipped as near-duplicates of existing pages (see "Already attempted, skipped" below): **New Highs/New Lows Ratio** (already substantially covered by Market Breadth's "52-week new highs vs. new lows" chart) and **Equity Factor Performance** (already substantially covered by Factor Analysis's Market/Size/Value/Momentum Fama-French series).
* **Earnings Surprise History** (2026-09-13) — `/earnings-surprise.html`. Beat/meet/miss rates across the full S&P 500 (Alpha Vantage `EARNINGS`, which returns real multi-year quarterly history per call — unlike `EARNINGS_ESTIMATES`, no accumulating snapshot needed): beat-rate trend over the last ~12 calendarized quarters, sector breakdown for the latest well-covered quarter (beat rate and avg. surprise %, toggle between the two), surprise-magnitude distribution histogram, a pooled lag-1 "surprise persistence" test (beat-rate-after-a-beat vs. beat-rate-after-a-miss, two-proportion z-test with p-value) as the page's real-stats component, plus longest active beat/miss streak leaderboards and biggest-beat/miss-this-quarter tables. Backend: weekly `scheduled-surprise-background.js` (Saturday 11:10 UTC, after the rest of the Saturday sweep block) → Netlify Blobs (`earnings-surprise` store) → `earnings-surprise.js`, reusing Sector Beeswarm's `meta.json` for sector/name. Filters out quarters with a consensus estimate under 5¢ or a surprise beyond ±200% (divide-by-near-zero blowups, e.g. a real JPM 2009 print of a 367% "surprise") before they can distort a mean or leaderboard — see the file's own comments and the page's methodology blurb for the full rationale.
* **Dividend Growth Screener** (2026-09-13) — `/dividend-growth-screener.html`. Alpha Vantage `DIVIDENDS` (full historical ex-dividend record per call, going back decades for long-time payers) summed into calendar-year totals per S&P 500 constituent — frequency-agnostic, so it works the same for quarterly, semi-annual, or annual payers. Computes, per company: current consecutive-year growth streak (Dividend-Aristocrat-style, no claim about the formal 25-year threshold), 5-year dividend CAGR, and latest YoY growth. Page shows: market-wide median dividend growth trend over the last ~10 years, sector breakdown (avg. 5Y CAGR / avg. streak, toggle), YoY growth distribution histogram, and leaderboards for fastest growers, longest active streaks, and recent cutters. Backend: weekly `scheduled-dividend-growth-background.js` (Saturday 12:00 UTC, after `scheduled-surprise-background`) → Netlify Blobs (`dividend-growth` store) → `dividend-growth.js`, reusing Sector Beeswarm's `meta.json`. Non-payers and payers with under 3 full calendar years of history are excluded entirely — this page only screens established payers, so expect universe coverage well under the full ~500 (most of the S&P doesn't pay a dividend, or hasn't for 3+ years).
* **International vs. US Relative Performance** (2026-09-14, `nightly/2026-09-14-international-vs-us`) — `/international-vs-us.html`. Relative strength of developed-ex-US (EFA) and emerging-markets (EEM) equities against the US (SPY), indexed since EEM's April 2003 inception: long-run ratio chart, trailing-return ladder, rolling 12-month relative-return oscillator, average monthly spread return by dollar regime (Dollar Strengthening/Flat/Weakening, classified off the trailing 3-month change in EUR/USD), and a Pearson+Spearman regression of monthly relative return against the month's EUR/USD change — architecture directly mirrors Small Cap vs. Large Cap Spread. Backend: daily `scheduled-international-background.js` (weekdays 21:20 UTC — only 4 Alpha Vantage calls, no heavy pacing needed) → Netlify Blobs (`international-us` store) → `international-us.js`. Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` (EFA/EEM/SPY, full history) + `FX_MONTHLY` (EUR/USD). Tested locally with a synthetic dataset (designed positive EUR/USD-vs-relative-return correlation) served through a local stub API, driven with Playwright (dark/light/mobile) — no bugs found.
* **Analyst Estimate Dispersion** (2026-09-14, `nightly/2026-09-14-international-vs-us`, stacked on top of the International vs. US Relative Performance commit above) — `/analyst-estimate-dispersion.html`. How much analysts disagree with each other about a stock's current-fiscal-year EPS (relative high/low range around the consensus average, from the same `EARNINGS_ESTIMATES` payload Earnings Revisions uses) — a distinct question from whether the consensus itself is moving. Sector-by-sector median dispersion, a distribution histogram across the S&P 500, a market-median history that accumulates weekly, a Pearson+Spearman test of whether dispersion coincides with more analyst revision activity, and most-disagreement/most-consensus leaderboards (min. 5 covering analysts). Backend: new weekly `scheduled-dispersion-background.js` (Saturday 11:10 UTC, after Share Count Trends' sweep) → Netlify Blobs (`analyst-dispersion` store) → `dispersion.js` — a standalone full-S&P-500 `EARNINGS_ESTIMATES` sweep kept separate from `scheduled-revisions-background.js` (Earnings Revisions' own job) so this new page doesn't touch that already-shipped page's backend, at the cost of a second, otherwise-redundant sweep of the same endpoint. Reuses Sector Beeswarm's `meta.json` for company name/sector. Tested locally with a synthetic dataset (designed sector-level dispersion spread and a positive dispersion-vs-revision-churn correlation), driven with Playwright (dark/light/mobile) — no bugs found.
* **Shareholder Yield: Buybacks vs. Dividends** (2026-09-14, `nightly/2026-09-14-international-vs-us`, stacked on top of the Analyst Estimate Dispersion commit above) — `/shareholder-yield.html`. Combines dividend yield and buyback yield into one "shareholder yield" number across the S&P 500 — the backlog's "Buyback Yield vs. Dividend Yield" idea — plus a Pearson+Spearman test of the common claim that dividend-payers and buyback-heavy companies are different populations (mature/stable cash flow → dividends; more variable earnings → discretionary buybacks). Stacked-bar sector view, a dividend-yield-vs-buyback-yield scatter with regression, and three leaderboards (highest combined, highest dividend-only, highest buyback-only). Backend: new weekly `scheduled-shareholder-yield-background.js` (Saturday 11:25 UTC, after both Share Count Trends' and Analyst Estimate Dispersion's sweeps) → Netlify Blobs (`shareholder-yield` store) → `shareholder-yield.js`. Dividend yield comes from a dedicated `COMPANY_OVERVIEW` sweep (its `DividendYield` field is already trailing and price-normalized — no separate price series needed). Buyback yield is deliberately *not* re-derived from `BALANCE_SHEET`: it's read straight off Share Count Trends' already-computed trailing-12-month share-count change (negated), reusing that page's existing weekly output instead of a second, redundant `BALANCE_SHEET` sweep of the same data — the one place this session's three new pages read (not just metadata-join) another page's own computed output rather than raw Alpha Vantage data. Tested locally with a synthetic dataset (designed mild negative dividend/buyback correlation to exercise the "substitution" story), driven with Playwright (dark/light/mobile) — no bugs found.
* **Stock Split Tracker** (2026-09-14, this session) — `/stock-split-tracker.html`. Split activity across the full S&P 500: a 15-year frequency history (forward vs. reverse splits — is the split coming back after two quiet decades?), sector activity in the trailing 5 years, and an event study of relative price performance vs. SPY in the trading days around each recent forward split (a Pearson+Spearman regression of split ratio size against 120-day post-split relative return). Data: Alpha Vantage `SPLITS` (full-universe sweep) + `TIME_SERIES_DAILY_ADJUSTED` (only for the smaller subset of tickers with a split in the last 5 years). Found and filtered a real data-quality issue: `SPLITS` mixes in non-split spin-off adjustment factors (GE's 2023/2024 spin-off ratios showed up as fake "splits") — see `netlify/functions/scheduled-splits-background.js` for the ratio-allowlist filter this required. Backend: weekly `scheduled-splits-background.js` (Saturday 12:20 UTC — moved from its original 11:10 UTC after a same-minute collision with `scheduled-surprise-background`, from a concurrently-built PR, was caught and fixed while merging that PR into `main`).
* **Forward vs. Trailing P/E Divergence** (2026-09-14, this session) — `/pe-divergence.html`. The market's own implied year-ahead EPS growth rate, read directly off `TrailingPE / ForwardPE - 1` (both ratios share the same price numerator, so no separate EPS-estimate data is needed) — sector medians, a distribution histogram, an accumulating weekly history (Alpha Vantage `OVERVIEW` is a snapshot, so this builds up real history one run at a time, same pattern as Earnings Revisions), and a Pearson+Spearman regression testing whether last quarter's actual EPS growth predicts the market's implied forward growth. Data: Alpha Vantage `OVERVIEW` (full-universe sweep, its own standalone pass rather than reusing Sector Beeswarm's weekly `OVERVIEW` sweep — see that page's methodology section for why). Backend: weekly `scheduled-pe-divergence-background.js` (Saturday 12:45 UTC — moved from its original 11:40 UTC for the same reason as Stock Split Tracker's schedule note above).
* **Relative Strength Leaders/Laggards** (2026-09-14, branch `claude/bold-clarke-oi26nk`) — `/relative-strength-leaders.html`. 1-month and 3-month price momentum relative to SPY across the full S&P 500: sector-aggregate bar chart, Leader/Neutral/Laggard tagging (top/bottom quintile of 3-month relative return), paired Leaders/Laggards tables, a full sortable per-stock table, and a forward-return persistence test (Pearson+Spearman) — deliberately built as "this week's rank vs. *next* snapshot's realized forward return," not "this week's rank vs. next week's rank," since two 63-trading-day windows one week apart share 58 days and would correlate almost mechanically regardless of real momentum (the same overlapping-window pitfall Share Count Trends' methodology calls out). Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED`, **compact outputsize** (not full) for all ~504 calls (503 constituents + SPY) — full-history outputsize was tested directly against Alpha Vantage during design (confirmed AAPL alone returns 6,756+ daily bars, ~580K tokens) and rejected as disproportionate to what a 21/63-trading-day lookback needs; this is a deliberate scope-down from a 1Y/6M ladder (like Small Cap vs. Large Cap Spread's) in favor of keeping the sweep the same cost/reliability class as the site's other ~503-call weekly jobs. Backend: new weekly `scheduled-relative-strength-background.js` (Saturday 13:10 UTC — moved from its original 12:00 UTC after a same-minute collision with `scheduled-dividend-growth-background`, from a concurrently-built PR, was caught and fixed while merging this PR into `main`; now runs after `scheduled-pe-divergence-background` at 12:45), reusing Sector Beeswarm's `meta.json` for company name/sector. Weekly snapshots (up to 20 retained) accumulate in a `history.json` blob for the persistence test — like Earnings Revisions and PE Divergence, it needs a week or two of real runs before that test has enough data, and shows a graceful "not enough history yet" message until then. Tested locally with a synthetic 503-company dataset (Playwright, dark/light/mobile, plus a deliberately-correlated persistence signal to confirm the forward-return test computes and renders correctly) through a local stub API and locally-vendored Chart.js (this sandbox's egress policy blocks the jsdelivr CDN the page loads Chart.js from in production, same issue noted by the prior session that built Stock Split Tracker/PE Divergence) — no bugs found.
* **Equity Risk Premium by Sector** (2026-09-14, branch `claude/bold-clarke-oi26nk`, same branch/PR as Relative Strength Leaders/Laggards above) — `/equity-risk-premium.html`. A simple "Fed model" equity risk premium proxy (trailing earnings yield − 10-year Treasury yield) across the full S&P 500 and its 11 GICS sectors — deliberately much cruder than `/implied-erp` (Damodaran's FCFE/DCF model, sourced not computed), and the methodology section says so explicitly rather than implying it's a competing "true" ERP number; chosen specifically because it's computable per-sector and per-stock from data Alpha Vantage already has, which the Damodaran number isn't. Sector median-ERP bar chart, a weekly-accumulating market-median-ERP trend line, Highest/Lowest-ERP leaderboards, a full sortable table, and a single-snapshot cross-sectional test (no cold-start wait, unlike the forward-return tests on other new pages this run) of whether the market prices risk rationally — regressing each stock's ERP against its Alpha Vantage-reported beta, Pearson+Spearman. Data: Alpha Vantage `COMPANY_OVERVIEW` (full ~503-ticker sweep for trailing P/E and beta) + one `TREASURY_YIELD` call. Backend: new weekly `scheduled-equity-risk-premium-background.js` (Saturday 13:35 UTC — moved from its original 12:30 UTC, which landed mid-window of `scheduled-splits-background`'s sweep from a separate, concurrently-built PR; now runs after `scheduled-relative-strength-background` at 13:10, once that job's own unrelated schedule collision was fixed too — see both functions' own schedule comments). Deliberately reads name/sector off each stock's own `COMPANY_OVERVIEW` response (same `normalizeSector` GICS-normalization helper Sector Beeswarm's own metadata job uses) rather than joining against another page's blob, since this job already pays for the full sweep. Companies with a zero/negative trailing P/E are excluded, not clamped, since a negative P/E doesn't imply a meaningful earnings yield at all. Tested locally the same way as this session's other page (synthetic 503-company dataset, Playwright, dark/light/mobile with the `localStorage.theme` fix, local stub API + vendored Chart.js) — no bugs found.
* **Earnings Growth vs. Price Performance Divergence** (2026-09-14, branch `claude/bold-clarke-oi26nk`, same branch/PR as the two pages above) — `/earnings-growth-divergence.html`. Trailing year-over-year quarterly EPS growth (Alpha Vantage `EARNINGS`, latest quarter vs. the same quarter 4 quarters back, to avoid seasonal noise) vs. 3-month price performance relative to SPY. Deliberately does **not** run a second full-universe price sweep: it reads this same night's Relative Strength Leaders/Laggards `latest.json` blob directly (`getRelativeStrengthStore`) for the price half of the comparison — a real, documented cross-page dependency (scheduled at 14:00 UTC — moved from its original 13:00 after both Relative Strength Leaders/Laggards and Equity Risk Premium by Sector were themselves rescheduled to resolve unrelated collisions during this PR's merge into `main`; runs after Relative Strength Leaders/Laggards, now at 13:10, and Equity Risk Premium by Sector, now at 13:35, specifically so that data is fresh same-day), with a graceful EPS-growth-only fallback (and an on-page warning banner, tested directly) if that blob isn't populated yet. Sector-paired bar chart, an EPS-growth-vs-price-return scatter with Pearson+Spearman, and two "divergence score" leaderboards (rank-by-EPS-growth minus rank-by-price-return, in both directions) — Growth Ahead of Price / Price Ahead of Growth. Companies with a loss in the year-ago quarter are excluded from the growth figure (not shown as a distorted or infinite %), same convention as Equity Risk Premium's P/E exclusion. Name/sector come from Sector Beeswarm's `meta.json`, same as every other full-universe job on this site. Tested locally the same way as this session's other two pages, including a dedicated "relative-strength blob not yet populated" scenario to confirm the fallback path and warning banner both work — no bugs found.
* This was a three-page night (all in one PR/branch) rather than the usual one-or-two: Relative Strength Leaders/Laggards and Equity Risk Premium by Sector are independent full-universe sweeps: Earnings Growth vs. Price Performance Divergence deliberately builds on the first rather than adding a fourth ~503-call sweep, so the cost profile for the night is roughly two-and-a-half full sweeps, not three. Also worth noting for future runs: this session's testing caught a real methodology gap in how the *previous* two sessions' `light-desktop` Playwright variants were set up — this site drives its light/dark theme off a `localStorage` flag read at page load, not `prefers-color-scheme`, so a bare Playwright `colorScheme: "light"` context option (as used in the light-mode tests described in the Relative Strength/Small Cap/Sector Rotation/Share Count Trends entries above) never actually rendered the light theme — the "light-desktop" screenshots in those runs were dark-theme renders. No visual bug was found once corrected (this session re-verified both of this session's own new pages properly, via `page.addInitScript` setting `localStorage.theme = "light"` before navigation), but any future run relying on a *prior* run's "tested in light mode" claim for one of those four specific pages should treat it as unverified and spot-check directly rather than assuming it was actually covered.
* **Small-Cap Liquidity & Volume Trends** (2026-09-15, `nightly/2026-09-15-ipo-pipeline`) — `/small-cap-liquidity.html`. Amihud (2002) illiquidity ratio and Corwin-Schultz (2012) high-low bid-ask spread estimates for the smallest vs. largest quintile of S&P 500 constituents by market cap (a market-cap-within-the-S&P-500 proxy — explicitly flagged as not true Russell 2000 small caps, no free full small-cap-index constituent list was available), plus a Pearson+Spearman regression testing whether the Small/Mega liquidity gap widens in the same months small-caps underperform large-caps (reusing `/api/smallcap-largecap`'s existing monthly-return bucketing logic client-side). Backend: new weekly `scheduled-smallcap-liquidity-background.js` (Saturday 14:20 UTC — moved from its original 13:35 UTC, which the PR chose to clear only the jobs it could see at the time; that slot turned out to collide with `scheduled-equity-risk-premium-background` once every concurrently-built PR's own schedule fixes were applied — see that function's own schedule comment. ~200 sequential `TIME_SERIES_DAILY_ADJUSTED` calls, reuses Sector Beeswarm's `meta.json` for cohort membership/market cap — no new OVERVIEW sweep) → Netlify Blobs (`smallcap-liquidity` store) → `smallcap-liquidity.js`. Tested with Playwright (dark/light/mobile, real Chart.js served locally since this sandbox blocks jsdelivr) against a synthetic 200-ticker/504-day dataset — one real bug found and fixed pre-commit: the regression method table wasn't overflow-contained and pushed 371px of horizontal overflow at 390px width.
* **IPO Pipeline & Aftermarket Tracker** (2026-09-15, `nightly/2026-09-15-ipo-pipeline`) — `/ipo-pipeline.html`. Companion to (not a replacement for) `/ipo-activity.html`: tracks Alpha Vantage's forward-looking `IPO_CALENDAR` (~3 months out, CSV format), classifies each row Operating Company vs. Fund/Trust/SPAC by name pattern, and — since Ritter's historical dataset behind `/ipo-activity.html` has no per-IPO ticker to join price history against (confirmed dead end, see the Short Interest/IPO note below and PR #13's own investigation) — builds an honest **forward-only** aftermarket track record: once a tracked operating-company IPO's listing date passes, follows its day-1/5/10/20 return vs. SPY going forward from whenever this page first started tracking it. Explicitly does NOT attempt to annualize or forecast off the thin ~3-month calendar window (methodology calls this out as "a floor, not a forecast," with Ritter's trailing-5-year annual counts shown only as separate background context, never combined into one number). Backend: new weekly `scheduled-ipo-pipeline-background.js` (Saturday 13:20 UTC — trivial call volume: 1 `IPO_CALENDAR` pull + a handful of `TIME_SERIES_DAILY_ADJUSTED` compact pulls for roster entries due for a check) → Netlify Blobs (`ipo-pipeline` store) → `ipo-pipeline.js`. Cold-start page (pipeline-size trend and aftermarket-tracking table both start sparse/empty and build up over subsequent weekly runs) with graceful empty states for both. Tested with Playwright against synthetic mocks covering full/mixed, cold-start, and all-empty roster states — one real bug found and fixed pre-commit: the aftermarket table's "complete entries first" sort compared a row object to a string instead of `row.status`, silently degrading to a pure date sort.
* **Status as of 2026-09-16: all four other Equities PRs this session flagged as open/unmerged — #4, #12, #13, #14 — plus this run's own PR (#15) have since been rebased onto current `main` and merged, one at a time, resolving several real Saturday-cron collisions along the way (each affected function's own schedule comment documents the collision and the fix).** All pages from every PR are now live; no further merge-order action needed. This note is kept as the historical record.
* **Spin-Off Performance Tracker** (2026-09-15, `nightly/2026-09-15-spinoff-performance`) — `/spin-off-performance.html`. Tests the "spin-off effect" (spincos and their remaining parents tend to outperform for a stretch after separation, one common explanation being forced index-fund selling of the new, unfamiliar spinco) across 19 hand-compiled spinoff events since 2015 (IBM/Kyndryl, GE/GEHC, GE/GEV, DowDuPont/Dow, DowDuPont/Corteva, three Danaher spinoffs, 3M/Solventum, JNJ/Kenvue, Kellanova/WK Kellogg, Honeywell/Resideo, the HP split, eBay/PayPal, Xerox/Conduent, Merck/Organon, Pfizer/Viatris, and UTC's Otis/Carrier spinoffs — the latter two with no parent leg since UTC merged into Raytheon the same day). Event dates/tickers are hand-compiled from public record (same "curated metadata, real market data" split as `/expectations-vs-reality`); every return number is a real Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` pull (adjusted close) against SPY as the benchmark — nothing about the price data is fabricated. An event-time-aligned average cumulative excess-return curve (every leg indexed to its own separation date, N shrinking transparently at longer horizons), a one-sample t-test plus an exact two-sided sign test on excess return at the 1-year mark (run separately for spinco legs, n≈19, and parent legs, n≈17, matching /factor-analysis's two-method-check spirit and its honesty about small-sample limits), a spinco-vs-parent excess-return scatter with Pearson+Spearman regression, best/worst leaderboards, a sector breakdown, and a full sortable per-leg table. Backend: weekly `scheduled-spinoff-background.js` (Saturday 11:02 UTC — moved from its original 11:10 UTC after a same-minute collision with `scheduled-surprise-background`, from a concurrently-built PR, was caught and fixed while merging this PR into `main`; runs after `scheduled-share-count-background`, finishing ~11:00) — only ~33 unique tickers (19 spincos + 13 unique parent tickers, several reused across more than one event, + SPY), so minimal rate-limit risk — → Netlify Blobs (`spinoff-performance` store) → `spinoff-performance.js`. Any ticker/leg whose price history doesn't reach back to its event's anchor date is dropped gracefully (logged in `skippedLegs`) rather than fabricated or blocking the page; a few events carry footnoted nuances (Viatris is a Reverse-Morris-Trust spin-merger not a pure spinoff, Kenvue is anchored to the exchange-offer completion date not the earlier IPO carve-out, Kellanova/WK Kellogg's parent ticker kept trading under a new name). Tested locally by running the actual production background-job code against a mocked Alpha Vantage fetch (synthetic-but-realistic GBM price paths, real event dates/tickers, a deliberate strong-drift boost on a few spincos and a drag on a couple others, plus one ticker forced to fail every attempt) and a mocked blob store, served through a local stub API, driven with Playwright in dark/light desktop and dark/light mobile — caught and fixed one real bug pre-merge: the two method-summary tables (t-test/sign-test and the scatter's regression stats) sat directly in a `.chart-box` rather than a `.table-box`, so the site-wide `table{min-width:600px}` rule forced the whole page wider than a phone viewport; fixed by giving those two containers their own `overflow-x:auto`.
* **Tracking-list catch-up (2026-09-21)**: this list had gone stale — it stopped being updated around 2026-09-15/16, but the site kept growing (other sessions, not this routine's own logging, or logging that didn't make it into this file across some merge). As of this run, the following pages are live on `main` and were **not** previously logged here; recorded now so future runs don't re-derive or re-attempt them. One-line summaries only (a fuller catalog research pass, not this run's own build, produced these):
  - `/analyst-price-target.html` — analyst price-target dispersion/accuracy vs. actual returns. Data: Alpha Vantage `COMPANY_OVERVIEW`. One-time snapshot.
  - `/backtesting.html` — client-driven strategy backtester (moving-average/rule-based entries/exits). Data: Alpha Vantage daily adjusted time series, on-request (not a scheduled job).
  - `/buyback-effectiveness.html` — TTM buyback $ spend (`CASH_FLOW`) vs. actual net share-count shrinkage, plus a Fed-funds-regime test. **Does not** track announced dollar-value buyback programs (see the "Buyback Announcements" skip note below, still open). Data: Alpha Vantage `CASH_FLOW`, `FEDERAL_FUNDS_RATE`, `COMPANY_OVERVIEW`, `BALANCE_SHEET`. One-time snapshot.
  - `/congressional-trading.html` — congressional stock trading activity (net buy/sell by member/sector) vs. relative strength. Data: Alpha Vantage `CONGRESS_TRADES`. One-time snapshot.
  - `/country-sector-scorecard.html` — heat-mapped region/style/sector total returns by month/quarter/year using ETF proxies. Data: `/api/country-performance` + `/api/sector-performance`. Daily job (`scheduled-countries-background`, weekdays 21:45 UTC). Passcode-gated as unlisted in-progress work.
  - `/earnings-call-sentiment.html` — earnings call transcript sentiment vs. subsequent stock performance. Data: Alpha Vantage `EARNINGS`, `EARNINGS_CALL_TRANSCRIPT`. One-time snapshot.
  - `/fcf-yield.html` — free cash flow yield ranking by sector (`operatingCashflow - capitalExpenditures`) vs. bottom-up earnings yield, FCF-conversion earnings-quality scatter. Data: Alpha Vantage `CASH_FLOW`. One-time snapshot (2026-09-17). Reused directly by this run's AI Capex Tracker for the capex sign/field convention.
  - `/institutional-ownership.html` — 13F institutional ownership levels/flow trends by company/sector. Data: Alpha Vantage `INSTITUTIONAL_HOLDINGS`. **Weekly recurring**, Saturday 11:10 UTC.
  - `/news-sentiment.html` — news sentiment/attention vs. forward relative return. Data: Alpha Vantage `NEWS_SENTIMENT`. One-time snapshot (2026-09-18).
  - `/post-earnings-drift.html` — post-earnings-announcement drift (PEAD) event study. Data: earnings-surprise snapshot + price sweep. **Weekly recurring**, Saturday 11:30 UTC.
  - `/quality-lowvol-factors.html` — quality/low-volatility factor combination vs. forward relative return; this is what closes out the "Quality and low-vol specifically aren't covered" caveat in the Equity Factor Performance skip note below. Data: Margin & Leverage Cycle + Relative Strength outputs, plus Yahoo Finance. **Weekly recurring**, Saturday 13:40 UTC.
  - `/quality-score.html` — Piotroski-style F-Score quality ranking by sector. Data: Alpha Vantage `BALANCE_SHEET`, `INCOME_STATEMENT`, `CASH_FLOW` (two-stage job, see `quality-financials-blob-store.js`). One-time snapshot.
  - `/seasonality.html` — calendar/seasonal return heat map (day-of-week, month) for the S&P 500. Data: Yahoo Finance daily close history for ^GSPC. Daily job, weekdays 21:50 UTC.
  - `/sector-beeswarm.html` — daily and annual bubble-chart visualization of per-company/sector returns vs. SPY, sized by market cap. Data: Alpha Vantage. Three scheduled jobs (meta, daily 22:40 UTC weekdays, annual). This is also the page whose `meta.json` blob nearly every other full-universe job on the site (including this run's AI Capex Tracker) reuses for company name/sector.
  - `/short-sale-volume.html` — short-sale-*volume* ratio (SVR) vs. forward relative return. **Explicitly not** FINRA short interest (see the "Short Interest Tracker" skip note below, still open) — uses FINRA's free Reg SHO daily short-sale-volume ("CNMS") file instead. Data: FINRA Reg SHO + Yahoo Finance. **Weekly recurring**, Saturday 14:50 UTC.
  - `/statistical-engine.html` — "Oddstats" streak scanner (unusual statistical streaks and their historical forward-return analogs). Data: generated by `node scripts/oddstats-detect.js --dump` from existing site data, no dedicated backend job. Password-gated.
* **Open, unmerged as of 2026-09-21: PR #31** (`claude/sharp-hopper-n7le6r`, "Add three new Equities pages: ROIC vs. WACC, Cash Conversion Cycle, R&D Intensity") adds `/roic-wacc.html`, `/cash-conversion-cycle.html`, `/rd-intensity.html` plus their backend jobs and the usual site-wide nav bump. Not reviewed or built by this run — noted here purely so this run (and the next one) doesn't duplicate those three ideas. Check whether it's since merged before starting new work on ROIC/WACC, cash conversion cycle, or R&D intensity.
* **AI Capex Tracker** (2026-09-21, this session) — `/ai-capex-tracker.html`. Off-list idea (per this brief's standing permission to propose beyond the backlog), added under "Earnings & Fundamentals" where the site already carried a "coming soon" placeholder for it. Tracks capex growth and capex intensity (capex ÷ revenue) across the full S&P 500, with a hand-curated six-name "hyperscaler + AI infrastructure" cohort (Microsoft, Alphabet, Amazon, Meta, Oracle, Micron — chosen for capex specifically attributable to AI/datacenter buildout, deliberately excluding largely-fabless chip designers like Nvidia/Broadcom/AMD whose *revenue* is AI-exposed but whose *capex* isn't where that spending shows up) broken out against the rest of the index. Core analysis: a structural-break (Chow) test on aggregate capex intensity's trend over time — equal-length oldest-half vs. most-recent-half split, same discipline as `/factor-analysis`'s momentum subsample check — run separately for the AI cohort and the rest of the index, to test the "AI capex supercycle" claim as an actual trend break rather than eyeballing a chart. Also a cross-sectional Pearson+Spearman test of TTM capex growth vs. TTM revenue growth (is elevated spending showing up as revenue, or running ahead of it), a sector breakdown, and leaderboards/full table. Data: Alpha Vantage `CASH_FLOW` (new ~503-call quarterly sweep, 28 quarters/~7 years, for `capitalExpenditures`) joined by fiscal-quarter-end date against the 28-quarter `totalRevenue` history `scheduled-margin-leverage-background.js` already keeps in its own checkpoint blob — deliberately **not** a second `INCOME_STATEMENT` sweep, so this page pays for one new full-index sweep instead of two. Backend: new `scheduled-ai-capex-background.js`, one-time snapshot (no recurring schedule, matching the convention every page added since 2026-09-16 settled into), manual run required after `scheduled-margin-leverage-background` has populated its checkpoint at least once. Tested locally with a synthetic 34-company dataset (a deliberately-planted trend break in the cohort's capex-intensity series, a flat trend for the rest of the index, and a positive capex-growth/revenue-growth correlation) via a local static-file stub (Chart.js and the zoom plugin vendored from npm, since this sandbox's egress policy blocks the jsdelivr CDN the page loads them from in production — same issue prior sessions hit), driven with Playwright across dark/light desktop and dark/light mobile. Caught and fixed two real bugs pre-commit: a mismatched-quote JavaScript syntax error in the trend-break table's renderer (opened a string with `"`, closed it with `'` — passed `node --check` on the wrong extraction the first time; caught with `acorn`'s parser giving an exact error position), and the trend-break method table not being overflow-contained, pushing ~480px of horizontal overflow at a 390px mobile viewport (same class of bug flagged in `/spin-off-performance.html`'s and `/margin-leverage.html`'s own methodology comments — fixed the same way, an explicit `overflow-x:auto` on the table's container).
* **Options Positioning** (2026-09-21, this session, second page of the night) — `/options-positioning.html`. Fills the "Options Positioning" coming-soon placeholder under Ownership & Flows. Full-option-chain put/call ratio across the S&P 500 (`HISTORICAL_PUT_CALL_RATIO`'s `put_call_ratio_full_chain` field — one call per company returns a single aggregate ratio for that name's whole option chain on the latest session, not a per-contract breakdown). `HISTORICAL_VOLUME_OPEN_INTEREST_RATIO` was considered too and rejected: it returns thousands of individual-contract rows per company with no aggregate figure (~3,500 rows for AAPL alone in a design-time test call), disproportionate to a cross-sectional positioning page. Median (not mean — the ratio is right-skewed by a handful of thin option chains) and mean put/call ratio market-wide and by sector, a market-median history that accumulates one point per run (same cold-start pattern as Earnings Revisions/PE Divergence, since the endpoint is a current-state snapshot not a queryable time series), a Pearson+Spearman cross-sectional test of put/call ratio against each company's trailing 3-month return relative to SPY (joined from Relative Strength Leaders/Laggards' `latest.json`, same reuse pattern as Earnings Growth vs. Price Performance Divergence, with the same graceful null-fallback if that blob is stale), and most-bearish/most-bullish leaderboards plus a full sortable table. Backend: new `scheduled-options-positioning-background.js`, one-time snapshot (no recurring schedule). ~503 sequential `HISTORICAL_PUT_CALL_RATIO` calls at 1050ms spacing plus a retry pass, reusing Sector Beeswarm's `meta.json`. Tested locally with a synthetic 50-company dataset (a deliberately-planted negative put/call-vs-relative-return correlation) via the same local static-file stub + npm-vendored Chart.js pattern as this session's first page. Caught and fixed one real bug pre-commit: two stat-cards show a sector name (e.g. "Consumer Discretionary") rather than a number, and at the template's default 24px monospace `.stat-value` size that text forced a ~25px CSS Grid column blowout on a phone-width `.stat-grid` — fixed with a `.stat-value-text` modifier class (smaller, sans-serif) for those two cards specifically, plus a defensive `min-width:0` on `.stat-card` generally.
* **Status as of 2026-09-20: this session surveyed the whole backlog above (all 24 original ideas) and found every one already built or explicitly skipped** — see the per-item notes throughout this list and "Already attempted, skipped" below. The site has also grown well beyond the original backlog since it was written (buyback effectiveness, congressional trading, country/sector scorecard, FCF yield, institutional ownership, post-earnings drift, quality score, quality/low-vol factors, seasonality, sector beeswarm, short sale volume, news sentiment, earnings call sentiment, analyst price target, backtesting, statistical engine — none of these were ever logged here, so this note exists so a future run doesn't mistake this list for the full site). A future run picking up this brief should propose a fresh, off-list idea (as this session did) rather than re-scanning the original 24 for gaps.
* **ROIC vs. Cost of Capital** (2026-09-20, this session) — `/roic-wacc.html`. A fresh, off-list idea (per this brief's explicit permission to propose beyond the listed backlog): is the S&P 500 actually earning more than its cost of capital, company by company? Computes trailing-twelve-month NOPAT (EBIT &times; (1 &minus; effective tax rate), Alpha Vantage's direct `ebit` field falling back to operating income) over invested capital (book debt + book equity &minus; cash) for ROIC, and a CAPM-based WACC (cost of equity = risk-free rate + beta &times; a market ERP assumption; cost of debt = TTM interest expense / debt, after-tax; book-value weights). Sector ROIC-vs-WACC grouped-bar chart, a spread distribution histogram, and two single-snapshot Pearson+Spearman regressions (spread vs. beta — does the market's own risk-return logic show up in realized profitability; spread vs. 3-month relative price return — does the market reward current value creation), plus value-creator/value-destroyer leaderboards and a full sortable table. Deliberately runs its own independent ~1006-call `INCOME_STATEMENT` + `BALANCE_SHEET` sweep rather than extending `scheduled-margin-leverage-background.js`'s existing checkpoint (that checkpoint's `KEEP_FIELDS` don't carry interest expense, tax figures, or shareholder equity, and scheduled-quality-financials-background.js already depends on that checkpoint's current shape — extending it felt riskier than a second sweep of the same two endpoints). Reuses beta and the market-median earnings-yield-based ERP from `/equity-risk-premium.html`'s blob, and 3-month relative return from `/relative-strength-leaders.html`'s blob, both as optional read-only dependencies with graceful fallbacks (assumed beta of 1 and a fixed market-ERP constant) so this page is independently buildable even before those jobs have run. Companies with non-positive book shareholder equity (common after aggressive, sustained buybacks — McDonald's, Starbucks, etc.) are excluded rather than shown with a distorted ratio, flagged explicitly as a real limitation in the methodology section, not an edge case. One-time snapshot, no schedule — matches this file's convention for every full-universe Equities job added since 2026-09-16. Tested by running the actual production handler against a mocked `fetch` (a 33-company synthetic universe across all 11 sectors, with a deliberately negative-equity name to exercise the exclusion path and a deliberately-missing-interest-expense name to exercise the debt-cost fallback) and a mocked Netlify Blobs store, served through a local stub API with locally-vendored Chart.js/zoom-plugin (this sandbox's egress policy blocks the jsdelivr CDN the page loads from in production, same issue prior sessions have noted), driven with Playwright in dark/light desktop and dark/light mobile — no console errors, no horizontal overflow, no bugs found.
* **Cash Conversion Cycle** (2026-09-20, this session, same branch/PR as ROIC vs. Cost of Capital above) — `/cash-conversion-cycle.html`. A second fresh, off-list idea: how many days of cash the average S&amp;P 500 company has tied up in its own operating cycle (DSO + DIO &minus; DPO), sector by sector and company by company — a genuinely different question from anything else on the site (working-capital efficiency, not profitability, leverage, or a cost-of-capital estimate). TTM revenue and cost of revenue (Alpha Vantage's `costOfRevenue`, falling back to the oddly-cased `costofGoodsAndServicesSold` field) joined against the latest quarter's receivables/inventory/payables. Sector working-capital-days grouped-bar chart, a CCC distribution histogram, a DSO-vs-DPO scatter with a 45&deg; reference line and Pearson+Spearman regression, a second regression testing CCC against this session's own ROIC vs. Cost of Capital data (does tighter working capital actually show up as a higher return on invested capital), fastest/slowest leaderboards, and a full sortable table. Deliberately excludes Financials, Real Estate, and Utilities almost entirely — "inventory" and "cost of revenue" aren't meaningful line items for a bank, a REIT, or a power utility — flagged explicitly in the methodology rather than left as an unexplained coverage gap. Own independent ~1006-call `INCOME_STATEMENT` + `BALANCE_SHEET` sweep (same reasoning as ROIC vs. Cost of Capital: no existing two-statement checkpoint on this site carries inventory/receivables/payables/cost-of-revenue), with an optional read-only dependency on ROIC vs. Cost of Capital's own published snapshot for the cross-page regression. One-time snapshot, no schedule. Tested the same way as ROIC vs. Cost of Capital: the real production handler against a mocked `fetch` (33-company synthetic universe, 11 sectors, with Financials/Real Estate/Utilities given no inventory/COGS data to exercise the exclusion path, and a subset of fast-turn names to exercise negative CCC) and a mocked Netlify Blobs store (including a pre-seeded ROIC blob), served through the same local stub API + vendored Chart.js, Playwright dark/light desktop and dark/light mobile — no console errors, no overflow, no bugs found.
* **R&D Intensity** (2026-09-20, this session, same branch/PR as ROIC vs. Cost of Capital and Cash Conversion Cycle above) — `/rd-intensity.html`. A third fresh, off-list idea this session: how much of revenue S&amp;P 500 companies reinvest in research &amp; development, which sectors actually spend on it, and whether heavier spending shows up in revenue growth or relative price performance. TTM R&amp;D &divide; TTM revenue, plus a smoothed year-over-year TTM revenue growth figure (needs 8 quarters of `INCOME_STATEMENT` history: 4 for this year's TTM, 4 for last year's). Deliberately a single-endpoint sweep (~503 calls, no checkpoint needed, lighter than the two-statement sweeps the other two new pages this session needed) since `researchAndDevelopment` and `totalRevenue` both live on `INCOME_STATEMENT`. Sector R&amp;D-intensity bar chart, a distribution histogram, and two single-snapshot Pearson+Spearman regressions (R&amp;D intensity vs. revenue growth — a contemporaneous association, explicitly not framed as predictive, since R&amp;D typically takes years to show up in revenue; R&amp;D intensity vs. 3-month relative return, reusing Relative Strength Leaders/Laggards' blob), highest-intensity/fastest-growth leaderboards, and a full sortable table. Companies that don't report R&amp;D in all four trailing quarters are excluded entirely rather than treated as zero spenders — this naturally concentrates the page's coverage in Information Technology, Health Care, and Communication Services, flagged explicitly in the methodology as the finding itself, not an artifact to correct for. One-time snapshot, no schedule. Tested the same way as this session's other two pages: the real production handler against a mocked `fetch` (33-company synthetic universe, 11 sectors, R&amp;D-heavy sectors given a high probability of reporting R&amp;D and the rest a low one, to exercise both the inclusion and exclusion paths realistically) and a mocked Netlify Blobs store (including a pre-seeded relative-strength blob), served through the same local stub API + vendored Chart.js, Playwright dark/light desktop and dark/light mobile — no console errors, no overflow, no bugs found.
* **Margin & Leverage Cycle** (2026-09-15, `nightly/2026-09-15-margin-leverage`) — `/margin-leverage.html`. A fresh, off-list idea (per this brief's explicit permission to propose beyond the listed backlog) tracking S&P 500 corporate profitability and balance-sheet leverage through the recent Fed rate cycle — no existing page touches income-statement margins or balance-sheet leverage ratios. Sector-median quarterly trend (toggle: gross/operating/net margin) and sector-median net-debt/trailing-twelve-month-EBITDA trend across the 11 SPDR sectors; the core "why this matters" test aggregates to one point per calendar quarter (the cross-company median QoQ change in operating margin, and separately in net-debt/EBITDA) rather than a ~500-way pseudo-replicated company panel, then compares those by Fed-funds regime (Hiking/Holding/Cutting, using the identical trailing-3-month-change classification already built for `/small-cap-vs-large-cap.html`) and regresses each against the quarter's Fed-funds change (Pearson+Spearman, matching `/factor-analysis`'s two-method convention) — honestly reported as a small-sample test (only a few dozen quarters of history) given how sticky/slow-moving margins and leverage are; in this run's synthetic-but-realistic test fixture the planted signal showed cleanly (median margin change averaged about -0.40ppt/quarter in Hiking regimes vs. +0.20ppt/quarter in Cutting regimes), but that's a property of the test fixture, not a claim about the real production data, which will show whatever it actually shows once the real weekly sweep runs. Also a margin-vs-leverage cross-company scatter (most recent quarter, Pearson+Spearman), sector-relative leaderboards (widest/thinnest margin, most/least levered vs. each company's own sector median, not raw cross-sector), and a full sortable per-company table with a trailing-4-quarter margin trend arrow. Data: Alpha Vantage `INCOME_STATEMENT` + `BALANCE_SHEET` (quarterly, full S&P 500) + `FEDERAL_FUNDS_RATE` (monthly). Net debt prefers Alpha Vantage's combined `shortLongTermDebtTotal` field, falling back to summing `shortTermDebt`/`longTermDebt` when that's `"None"`; EBITDA prefers Alpha Vantage's direct `ebitda` field, falling back to `operatingIncome + depreciationAndAmortization` (same INCOME_STATEMENT report) when that's `"None"` — both fallback paths, and the case where even the fallback fields are missing (ratio left `null` rather than fabricated), are exercised by the test fixture. Backend: weekly `scheduled-margin-leverage-background.js` (Saturday 14:35 UTC — moved from its original 11:20 UTC, which directly overlapped `scheduled-shareholder-yield-background`'s own 11:25-~11:34 sweep, a separate, concurrently-built PR's job unaware of this one; now runs after `scheduled-smallcap-liquidity-background` at 14:20, the last job in the Saturday block as of this fix — see this function's own schedule comment in `netlify.toml` for the full account) — a two-statement sweep (~1006 Alpha Vantage calls) paced at 750ms between calls (tighter than the usual ~800-1050ms elsewhere in this codebase, specifically because doubling the calls-per-company nearly doubles total sweep time inside a Background Function's ~15-minute ceiling) plus a retry pass — reusing Sector Beeswarm's `meta.json` for company name/sector rather than a third full-index metadata sweep. Tested by running the actual production background-job handler against a mocked `fetch` (18 real-symbol tickers across 5 sectors with realistic synthetic quarterly financials built on a real-shaped 2019-2026 Fed-funds path, a deliberately-planted margin-compression/leverage-rise signal during the synthetic 2022-23 hiking stretch and partial recovery during 2024-26 cutting, one ticker whose every fetch attempt fails, and three tickers individually exercising the `shortLongTermDebtTotal`-missing, `ebitda`-missing, and both-EBITDA-inputs-missing fallback paths) and a mocked Netlify Blobs store, served through a local stub API (reusing this session's Chart.js/zoom-plugin vendor files and stub-server pattern already set up earlier this run for the Spin-Off Performance Tracker test), driven with Playwright in dark/light desktop and dark/light mobile. Caught and fixed one real layout bug pre-merge: the two-column `.chart-grid` used for the regime-comparison and rate-regression charts didn't protect its grid items from CSS Grid's default `min-width:auto` "blowout" behavior, so a method-summary table's intrinsic width forced the whole page wider than its container (worse on mobile, but present at desktop width too) — fixed with an explicit `min-width:0` on `.chart-grid > .chart-box` (and on `.method-table` itself, which was also inheriting the site-wide `table{min-width:600px}` rule meant for the main multi-column data tables). Opened in parallel with PR #16, Spin-Off Performance Tracker (`nightly/2026-09-15-spinoff-performance`) — both have since been rebased and merged into `main`, one at a time, resolving the real cron collision noted above along the way.

* **Status as of 2026-09-25: PRs #33, #34, and #35 are open/unmerged** — #33 (`SBC Dilution + Effective Tax Rate`), #34 (`Analyst Coverage + Net Cash Position`), #35 (`ROE Decomposition/DuPont`). Not reviewed or built by this run — noted here purely so this run (and the next one) doesn't duplicate those five ideas (SBC Dilution, Effective Tax Rate, Analyst Coverage, Net Cash Position, ROE Decomposition). Check whether they've since merged before starting new work on any of those five topics.
* **Earnings Quality via Accruals** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`) — `/accruals.html`. A fresh, off-list idea (per this brief's explicit permission to propose beyond the listed backlog, and confirmed against the then-open PRs #33/#34/#35 above as genuinely new, not a duplicate): Sloan's (1996) "accrual anomaly" — how much of reported net income is backed by real operating cash flow vs. non-cash accruals, and whether the market has historically underpriced that quality signal. A different, complementary lens to `/quality-score.html` (Piotroski F-Score) and `/roic-wacc.html`/`/margin-leverage.html` (profitability/leverage, not earnings quality). Total accruals = TTM net income &minus; TTM cash flow from operations, scaled by *average* total assets (latest-quarter total assets + total assets from ~4 quarters earlier, &divide; 2 — Sloan's own averaging construction, not a single point-in-time asset base) to get the accrual ratio. Sector median-accrual-ratio bar chart, a distribution histogram across the S&amp;P 500, a single-snapshot Pearson+Spearman regression of accrual ratio against each company's 3-month relative return (reusing <a href="/relative-strength-leaders.html">Relative Strength Leaders/Laggards</a>' blob, same optional-dependency/graceful-fallback pattern as `/roic-wacc.html`) as the core "does the anomaly show up" test — explicitly documented in the page's own methodology as a contemporaneous cross-section, not a true lagged/panel replication of Sloan's original multi-decade forward-return design — plus most-cash-backed/most-aggressive-accruals leaderboards and a full sortable table. Data: Alpha Vantage `CASH_FLOW` (TTM net income + operating cash flow) + `BALANCE_SHEET` (total assets, latest quarter and ~4 quarters back), the same ~1006-call two-statement sweep shape/cost as `/roic-wacc.html` and `/cash-conversion-cycle.html`. Backend: new `scheduled-accruals-background.js`, one-time snapshot (no recurring schedule, matching the convention every full-universe fundamentals page has used since 2026-09-16), paced at 750ms between calls with a checkpoint/resume pattern and a retry pass, structurally modeled on `scheduled-roic-wacc-background.js`. Reuses Sector Beeswarm's `meta.json` for company name/sector rather than a third metadata sweep. Companies missing the prior-period (~4-quarters-back) `BALANCE_SHEET` row needed for the averaging denominator are excluded entirely rather than falling back to a single point-in-time asset base, flagged explicitly in the methodology as a real, not-uncommon coverage gap (shorter Alpha Vantage history or a fiscal-calendar change) rather than a data-quality problem to paper over. Tested by running the actual production handler against a mocked `fetch` (a synthetic 35-company universe across all 11 GICS sectors, spread accrual ratios from about -12% to +12%, with a deliberately-planted strong negative accrual-ratio-vs-3-month-relative-return correlation, plus one dedicated ticker with a complete 4-quarter `CASH_FLOW` TTM window but only 4 `BALANCE_SHEET` quarters on file to exercise the missing-prior-period-balance-sheet exclusion path) and a mocked Netlify Blobs store (pre-seeded `beeswarm`/meta and `relative-strength`/latest blobs), served through a local stub API with locally-vendored Chart.js/zoom-plugin (this sandbox's egress policy blocks the jsdelivr CDN the page loads them from in production, same workaround prior sessions have documented), driven with Playwright across dark/light desktop and dark/light mobile (theme set via `page.addInitScript` writing `localStorage.theme`, not the Playwright `colorScheme` context option, per the documented mistake from an earlier session) plus the "backend hasn't run yet" 502/empty-state fallback. The synthetic run confirmed the exclusion path (1 of 35 tickers excluded, 34 published, all 11 sectors represented) and the regression rendering in the expected direction (Pearson r &asymp; -0.92, p&lt;0.001, on the planted signal — a synthetic-data sanity check only, not a claim about what the real S&amp;P 500 sweep will find once it runs post-merge). No bugs found in the new page itself; while updating every page's nav block for this addition (per this file's own duplicated-nav gotcha), also fixed three pre-existing nav-drift bugs noticed in that same block: `roic-wacc.html`, `cash-conversion-cycle.html`, and `rd-intensity.html` each wrongly marked the unrelated "Yield Curves" link `active` instead of their own page (now corrected to mark their own link active, matching their sibling pages), and `cash-conversion-cycle.html`'s own Earnings dropdown had silently stopped short of listing itself and R&amp;D Intensity (now brought in line with the canonical list every other page carries).
* **RSI Mean-Reversion / Short-Term Reversal Screen** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`, same branch/PR (#36) as Earnings Quality via Accruals above — second page of this multi-page night) — `/rsi-reversal.html`. A fresh, off-list idea and this site's first page built on any Alpha Vantage *technical-indicator* endpoint — every other page is fundamentals-, price-return-, or flow-based. Tests short-term reversal (Jegadeesh 1990; Lehmann 1990) — extreme recent price moves partially reverting over the following days/weeks — a different, opposite-sign phenomenon from `/relative-strength-leaders.html`'s existing 1&ndash;3 month momentum test. Core metric: 14-day RSI per S&amp;P 500 constituent from Alpha Vantage's `RSI` endpoint (daily interval, `time_period=14`, `series_type=close`) &mdash; a single-endpoint ~504-call sweep, same shape/cost class as `/rd-intensity.html`, only the latest value of the returned series used (same "full series returned, only last point used" pattern as `/stock-split-tracker.html`'s `SPLITS` calls). Page: current cross-sectional RSI histogram (30/70 oversold/overbought reference lines drawn via a small local Chart.js plugin, not a CDN annotation library), sector-median RSI bar chart, oversold/overbought leaderboards plus a full sortable table, a weekly-accumulating market-median-RSI history line (cold-start pattern matching `/earnings-revisions.html`/`/pe-divergence.html`/`/options-positioning.html`, since RSI is a current-state snapshot not a queryable historical cross-section), and the core test: each week's per-company RSI decile paired with that company's realized forward return relative to SPY over the following week, shown as a decile-bucket bar chart plus a pooled Pearson+Spearman regression of RSI level against forward relative return (expects a negative relationship if reversal is real). Deliberately a **real recurring weekly job** (Saturday 15:15 UTC &mdash; the last slot in the Saturday block as of this addition, after `scheduled-short-sale-volume-background` at 14:50), not a one-time snapshot like most fundamentals pages added since 2026-09-16, since the reversal test only exists because successive weekly snapshots can be compared. Forward returns are **not** computed from a second price sweep: each run reads (not writes) `/relative-strength-leaders.html`'s own weekly `history.json` (its `{date, spyPrice, prices}` points already exist for that page's own rank-persistence test) and copies that snapshot's price data into this job's own weekly history point, tagged with the source point's own date (`priceAsOf`); consecutive weekly points in *this* job's own history are then diffed the same way `scheduled-relative-strength-background.js` diffs its own points. A transition is skipped (not recorded as a fabricated zero-return week) rather than computed if the two points share the same `priceAsOf` &mdash; i.e. relative-strength hasn't produced a fresh weekly snapshot since this job's prior run &mdash; and the count of skipped transitions is surfaced on-page. Backend: new `scheduled-rsi-reversal-background.js` → Netlify Blobs (`rsi-reversal` store, `latest.json` + `history.json`) → `rsi-reversal.js`, reusing Sector Beeswarm's `meta.json` for company name/sector. Nav: added to the existing "Sectors &amp; Style" dropdown (all ~57 nav-carrying pages), immediately after Relative Strength Leaders/Laggards &mdash; deliberately *not* a new top-level nav category, per this session's own instructions, even though a "Momentum &amp; Technicals" grouping would have been a reasonable fit; also added to `index.html`'s homepage Research Areas grid in the matching "Sectors, Factors &amp; Style" card. Tested by running the actual production background-job handler three times in sequence (simulating 3 successive weekly runs, with a mocked `Date` so each run reports a distinct week) against a mocked `fetch` (a synthetic 36-company/11-sector universe &mdash; the same 35-ticker real-symbol roster `synthetic-data.js` already defines for the Accruals test, plus one dedicated retry-pass ticker &mdash; with a deliberately-planted negative RSI-vs-next-week-relative-return relationship) and a mocked Netlify Blobs store with an evolving `relative-strength` price history, served through a local stub API with locally-vendored Chart.js/zoom-plugin, driven with Playwright across dark/light desktop and dark/light mobile (theme via `page.addInitScript`/`localStorage.theme`), plus dedicated true-cold-start (1 snapshot only) and "backend hasn't run" 502/empty-state passes. Backend assertions after week 3: 72 pooled reversal pairs (well above the page's own 30-pair rendering threshold), Pearson r &asymp; -0.997 on the planted signal, decile 1 (most oversold) averaging +5.6% next-week relative return vs. decile 10 (most overbought) averaging -6.8% &mdash; a synthetic-data sanity check confirming the decile chart and regression both render in the expected (negative/reverting) direction, not a claim about what the real S&amp;P 500 sweep will find once it runs. Found and fixed one real bug during testing: the market-median-RSI history chart's cold-start (fewer than 2 weekly points) branch hid the canvas entirely behind a tall empty panel; changed to always render the chart (even a single point) with a small explanatory caption instead, matching `/earnings-revisions.html`'s own established convention for its history chart. No other bugs found.
* **52-Week High Momentum** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`, same branch/PR (#36) as Earnings Quality via Accruals and RSI Mean-Reversion above &mdash; third page of this multi-page night) &mdash; `/52-week-high-momentum.html`. **This page replaces an originally assigned idea, "Workforce Productivity &mdash; Revenue per Employee," which turned out to be a confirmed dead end** &mdash; see the "Already attempted, skipped" entry below for the full account before anyone picks that idea back up. Built instead: a fresh, off-list test of the "52-week high effect" (George &amp; Hwang, 2004) &mdash; do stocks trading near their 52-week high keep outperforming, rather than mean-revert? The opposite-signed cousin of `/rsi-reversal.html`'s short-term-reversal test on this same site, and architecturally its closest sibling (weekly-accumulating forward-return test off the same reused price source), so most of that page's structure carried over directly. Core metric: **range position** &mdash; `(price &minus; 52WeekLow) / (52WeekHigh &minus; 52WeekLow)`, 0% at the 52-week low to 100% at the 52-week high, clipped to that band for the rare stale-vs-live-price mismatch. `52WeekHigh`/`52WeekLow` come from a single-endpoint Alpha Vantage `OVERVIEW` sweep (~503 calls, same shape/cost as `/rd-intensity.html` and `/rsi-reversal.html`; confirmed present and correctly named on real test calls against AAPL/IBM/WMT/NVDA/CRWD, unlike `FullTimeEmployees` &mdash; see below). Current price (needed to place each stock within its own range, and for the forward-return test itself) is **not** a second price sweep: both reuse `/relative-strength-leaders.html`'s own weekly `history.json`, read (not written) and diffed the same way `/rsi-reversal.html` already does &mdash; identical reused-price-source/`priceAsOf`/stale-transition-skip pattern, just with `rangePos` swapped in for `rsi`. Page: current range-position distribution histogram (10%/90% reference lines), sector-median range-position bar chart, near-high/near-low leaderboards plus a full sortable table, a weekly-accumulating market-median-range-position history line, and the core test: each week's per-company range-position decile paired with that company's realized forward return relative to SPY over the following week, shown as a decile-bucket bar chart plus a pooled Pearson+Spearman regression (expects a **positive** relationship &mdash; the opposite sign from RSI Mean-Reversion's test &mdash; if the 52-week-high effect is real). Backend: new `scheduled-high52-momentum-background.js` → Netlify Blobs (`52-week-high-momentum` store, `latest.json` + `history.json`) → `high52-momentum.js`, reusing Sector Beeswarm's `meta.json` for company name/sector. Weekly recurring job, Saturday **15:35 UTC** &mdash; 20 minutes after `scheduled-rsi-reversal-background` (15:15 UTC), now the last slot in the Saturday block; staggered rather than run concurrently since both are independent ~503-call sweeps and a full sweep can take up to ~15 minutes worst-case with its own retry pass, same collision-avoidance discipline every recent page's schedule comment documents. Tested by running the actual production background-job handler three times in sequence (simulating 3 successive weekly runs, with a mocked `Date` so each run reports a distinct week) against a mocked `fetch` (a synthetic 35-company/11-sector universe with a deliberately-planted positive relationship between a company's current range position and its own forward drift, plus four dedicated edge-case tickers: one whose every `OVERVIEW` fetch attempt fails, one with a degenerate `52WeekHigh <= 52WeekLow` band, one missing from the beeswarm sector metadata, and one never present in the relative-strength price snapshot &mdash; exercising all four distinct exclusion paths separately) and a mocked Netlify Blobs store with an evolving `relative-strength` price history, served through a local stub API with locally-vendored Chart.js/zoom-plugin, driven with Playwright across dark/light desktop and dark/light mobile (theme via `page.addInitScript`/`localStorage.theme`), plus dedicated true-cold-start (1 snapshot only) and "backend hasn't run" 502/empty-state passes. Backend assertions confirmed all four edge cases excluded correctly (35 of 39 synthetic tickers published each run) and, after week 3: 70 pooled momentum pairs (well above the page's own 30-pair rendering threshold), Pearson r &asymp; +0.999 on the planted signal, decile 1 (nearest the 52-week low) averaging -4.6% next-week relative return vs. decile 10 (nearest the 52-week high) averaging +4.7% &mdash; a synthetic-data sanity check confirming the decile chart and regression both render in the expected (positive/continuation) direction, not a claim about what the real S&amp;P 500 sweep will find once it runs. Found and fixed one real bug during testing: a long inline formula in a `<code>` block in the methodology section (`white-space:nowrap` by the site's default `.explainer code` style) overflowed the page by ~29px at a 390px mobile viewport; fixed by overriding `white-space:normal` on that one span rather than loosening the site-wide rule. No other bugs found.

* **Dividend Coverage & Cut Risk** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`, same branch/PR (#36) as Earnings Quality via Accruals, RSI Mean-Reversion, and 52-Week High Momentum above &mdash; fourth page of this multi-page night) &mdash; `/dividend-coverage.html`. **This page replaces the night's originally-assigned fourth idea, "Buyback Timing Skill"** (does heavier quarterly buyback $ intensity predict better or worse subsequent stock performance &mdash; the Ikenberry/Lakonishok/Vermaelen 1995 long-run buyback-performance question), which was investigated first per this session's required duplication check and deliberately not built. Full reasoning: the proposal's own text suggested checking whether `/buyback-effectiveness.html`'s existing checkpoint blob could be read as an optional dependency instead of re-sweeping `CASH_FLOW` from scratch. Reading that page's backend (`scheduled-buyback-effectiveness-background.js`) end to end confirmed it cannot &mdash; that job fetches 24 quarters of per-company buyback spend (`quarters` on each company object) purely to build its TTM aggregate and a market-wide *cross-sectional* quarterly median (`marketQuarterly`, one point per calendar quarter across the whole universe), then explicitly strips the per-company `quarters` array before the final `payload.companies.map(...)` write &mdash; only TTM figures survive into the stored blob, never a per-company quarterly time series. A real timing-skill test needs exactly that per-company quarterly granularity, joined against each company's own subsequent 3&ndash;6 month return from every historical quarter-end &mdash; which would need not just a second, from-scratch `CASH_FLOW` sweep that keeps the full quarterly history (rather than reusing anything), but *also* a multi-year historical price sweep per company (`/relative-strength-leaders.html`'s own blob deliberately trims to ~100 trading days precisely because that page's own design doesn't need more, so it has no historical joinable series either). That's roughly double the Alpha Vantage cost of any other single page this site has built, for a resulting page whose core question (is the market's buyback-timing behavior smart or dumb money) sits close enough to Buyback Effectiveness's own Fed-funds-regime timing test that it reads as a natural extension of that already-live page rather than a clearly separate one &mdash; and this routine's boundaries say not to modify that page. Pivoted instead, per this brief's standing permission to substitute a fresh idea when a proposed one turns out to be a confirmed dead end (same pattern as the Revenue-per-Employee &rarr; 52-Week High Momentum substitution earlier the same night) to a genuinely different capital-returns question that, after surveying the existing Capital Returns cluster, no page on the site actually asks: is the dividend safe, i.e. actually covered by real cash, not just by reported earnings? `/dividend-growth-screener.html` tracks whether it's grown (streak, CAGR); `/shareholder-yield.html` tracks combined yield magnitude; neither tests coverage. Core metric: two TTM payout ratios computed from the same `CASH_FLOW` window and the same dividend numerator (`dividendPayoutCommonStock`, confirmed populated on real test calls against JPM/AAPL/O before writing any code, per this session's data-sourcing discipline) &mdash; an earnings payout ratio (&divide; net income, the "accounting" view) and an FCF payout ratio (&divide; (operating cash flow &minus; capex), the "can they actually afford it" view) &mdash; applying the same reported-earnings-vs-real-cash lens as this same night's `/accruals.html`, but specifically to dividend sustainability. `COMPANY_OVERVIEW`'s documented-sounding `PayoutRatio` field was checked and confirmed absent from a real JPM response, so both ratios are derived bottom-up from `CASH_FLOW` alone rather than assumed. Page: sector-median FCF-payout-ratio bar chart, a distribution histogram (100% reference line marking where the dividend stops being covered by trailing FCF), a self-contained earnings-vs-FCF payout ratio scatter with a 45&deg; reference line and Pearson+Spearman regression (mirrors Buyback Effectiveness's own dollar-yield-vs-net-yield offset scatter, just applied to dividends), a second scatter regressing FCF payout ratio against 3-month relative return (reusing `/relative-strength-leaders.html`'s blob, same optional-dependency/graceful-fallback pattern as `/roic-wacc.html` and `/rd-intensity.html`) testing whether the market already prices in a stretched payout ratio as a warning sign &mdash; both single-snapshot, contemporaneous cross-sections, explicitly described as such rather than implied to be predictive &mdash; plus best-covered/most-stretched/widest-coverage-gap leaderboards and a full sortable table. Companies with zero reported dividend payout across all 4 trailing quarters are treated as non-payers and excluded entirely (matching Dividend Growth Screener's own established-payers-only convention); companies with non-positive TTM net income or non-positive TTM FCF get that specific ratio shown as "neg." rather than a nonsensical or fabricated number, flagged as a real (and itself risk-relevant) case in both the table and the methodology, not papered over. Data: Alpha Vantage `CASH_FLOW` (new single-endpoint ~503-call TTM-only sweep &mdash; only 4 quarters needed, no checkpoint required, same shape/cost class as `/rd-intensity.html` and lighter than Buyback Effectiveness's own 24-quarter pull). Backend: new `scheduled-dividend-coverage-background.js`, one-time snapshot (no recurring schedule, matching the convention every full-universe fundamentals page has used since 2026-09-16), reusing Sector Beeswarm's `meta.json` for company name/sector. Nav: added to the existing "Capital Returns" dropdown (all ~59 nav-carrying pages, via a scripted find/replace verified against every file afterward for exactly one insertion each, zero duplicates/misses) immediately after Dividend Growth Screener, plus `index.html`'s homepage Research Areas grid in the matching "Capital Returns & Corporate Actions" card. `node scripts/build-schedule-page.js` re-run afterward to regenerate `data-pull-schedule.html` (confirmed the new manual job listed alphabetically with its correct data source and its `relative-strength` read-dependency noted). Tested by running the actual production background-job handler against a mocked `fetch` (a synthetic 35-company/11-sector universe with FCF payout ratios spread 20%&ndash;150% and a deliberately-planted negative FCF-payout-ratio-vs-3-month-relative-return relationship, plus five dedicated edge-case tickers: one whose every `CASH_FLOW` fetch attempt fails, one non-payer with `dividendPayoutCommonStock` reported "None" every quarter, one with negative TTM net income, one with capex exceeding operating cash flow (negative TTM FCF), and one missing from the beeswarm sector metadata &mdash; exercising all five distinct exclusion/flag paths separately) and a mocked Netlify Blobs store, run twice (once with the relative-strength dependency blob absent, once populated) to exercise both branches of that optional read, served through a local stub API with locally-vendored Chart.js/zoom-plugin (this sandbox's egress policy blocks the jsdelivr CDN the page loads them from in production, same workaround every prior session this week has documented), driven with Playwright across dark/light desktop and dark/light mobile (theme via `page.addInitScript`/`localStorage.theme`) plus the dependency-missing-warning-banner and "backend hasn't run yet" 502/empty-state scenarios. Backend assertions confirmed all five edge cases excluded/flagged correctly (37 of 40 synthetic tickers published: 35 normal + the negative-earnings and negative-FCF edge cases, with FAILTIX/the non-payer/the no-sector ticker correctly excluded) and the two planted correlations rendering in the expected directions (earnings-vs-FCF pairs: Pearson r &asymp; +1.00, by construction since the synthetic FCF ratio was built as earnings ratio &divide; 0.8; FCF-payout-vs-return pairs: Pearson r &asymp; -1.00 on the planted signal) &mdash; synthetic-data sanity checks confirming both scatters and regressions render correctly, not a claim about what the real S&amp;P 500 sweep will find once it runs post-merge. Found and fixed one real bug during testing: the two regression method-summary tables (`#coverageMethodTable`/`#returnMethodTable`, seven mono-font columns each) overflowed their `.chart-grid` column by 20px at a 390px mobile viewport even with `min-width:0` already set on both `.chart-grid .chart-box` and `.method-table` &mdash; the same recurring class of bug flagged in this file's own working-conventions section and hit by nearly every recent page; fixed with an explicit `overflow-x:auto` directly on the two method-table container divs, applied proactively (per this run's own instructions) rather than found later. No other bugs found.
* **Volatility Risk Premium** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`, same branch/PR (#36) as Earnings Quality via Accruals, RSI Mean-Reversion, 52-Week High Momentum, and Dividend Coverage &amp; Cut Risk above &mdash; fifth page of this multi-page night) &mdash; `/vol-risk-premium.html`. This session's assignment was an open-ended "find a genuinely fresh fifth idea or stand down" investigation, not a pre-picked topic. Three off-list candidates were investigated with real data checks before committing: (1) **Ownership concentration / 13F crowding (HHI)** &mdash; reusing the per-holder `holdings` array `INSTITUTIONAL_HOLDINGS` already returns but `/institutional-ownership.html`'s own methodology text says it deliberately doesn't use ("thousands of rows for a mega-cap... This page doesn't use it"); a real test call against AAPL confirmed why not &mdash; the full per-holder list has no size limit or pagination parameter and returned all 6,491 real holders in one response (459,139 tokens via the full-data path), meaning a full-universe sweep would carry a materially heavier and more variable per-call payload than any other job on this site, with real risk of blowing a Background Function's ~15-minute ceiling on the market's most widely-held names. Rejected on cost/reliability grounds, not a data-availability dead end &mdash; logged in "Already attempted, skipped" below in case a future session wants to revisit with a narrower approach (e.g. only the first N holders as returned, accepting an approximate rather than a true HHI). (2) **Capital structure / preferred stock exposure** &mdash; a real `BALANCE_SHEET` test call against Bank of America (a large, likely preferred-stock issuer) confirmed no preferred-stock field of any kind (value, shares, or otherwise) appears anywhere in the response; a firm data-availability dead end, logged below. (3) **Per-stock options-implied vs. realized volatility (the volatility risk premium)** &mdash; confirmed feasible and built. This site's `/volatility.html` compares the VIX to S&amp;P 500 realized volatility at the *index* level only (no per-stock cross-section), and `/options-positioning.html` reads the put/call ratio (a *directional* sentiment measure) rather than the *magnitude* of volatility options are pricing in &mdash; genuinely different from both. Alpha Vantage's `HISTORICAL_OPTIONS` endpoint (not previously used by any page on this site) was confirmed via real test calls to return a complete daily options chain per symbol with `implied_volatility` and full Greeks already computed per contract (a real AAPL call returned 3,564 rows/~65KB covering every expiration and strike; filtering to one target expiration via the `expiration` parameter cut that to ~120 rows) &mdash; no Black-Scholes solving needed. Core construction: for each S&amp;P 500 constituent, picks the expiration closest to 30 calendar days out (accepting 15&ndash;60 days; outside that band the company is excluded rather than compared on a mismatched horizon) and, within it, the call contract whose `delta` is closest to 0.50 &mdash; the standard model-free way to locate the at-the-money strike with no separate spot-price lookup needed at all &mdash; averaged with the same-strike put's IV when available. **ATM Implied Vol** = that value. **Realized Vol** = the annualized stdev of daily log returns over the trailing 20 trading days, from Yahoo Finance daily closes (`yahoo-client.js`, the same helper `/relative-strength-leaders.html` already uses) rather than a second Alpha Vantage sweep, since Yahoo carries none of Alpha Vantage's rate-limit exposure &mdash; this keeps the page's actual Alpha Vantage cost to one ~503-call single-endpoint sweep (`HISTORICAL_OPTIONS`), the same shape/cost class as `/rd-intensity.html` and `/rsi-reversal.html`, despite touching two data sources. **Volatility Risk Premium (VRP)** = ATM Implied Vol &minus; Realized Vol, testing the options-pricing literature's well-documented finding that VRP runs positive on average (options tend to overprice near-term risk) on this specific cross-section. Page: sector-median VRP bar chart, a VRP distribution histogram, an implied-vs-realized-vol scatter with a 45&deg; reference line plus Pearson+Spearman regression (the core test &mdash; the *intercept* reads as the average VRP once the recent-vol anchor is netted out), a second scatter regressing VRP against 3-month relative return (reusing `/relative-strength-leaders.html`'s blob, same optional-dependency/graceful-fallback pattern as `/roic-wacc.html` and `/rd-intensity.html`), richest/cheapest-VRP leaderboards, and a full sortable table. A company is excluded (not distorted) if no expiration falls in the 15&ndash;60 day band, if no call lands within 0.15 of a 0.50 delta (a strike grid too sparse/illiquid to call "at the money" with confidence), or if Yahoo can't supply a clean 21-close trailing window (recent IPO, halted/delisted name) &mdash; three distinct, separately-flagged exclusion counts (`excludedNoAtm`, `excludedNoRv`) surfaced on-page via the warning banner, not folded into a single opaque "N loaded" figure. Backend: new `scheduled-vol-risk-premium-background.js`, one-time snapshot (no recurring schedule, matching the convention every full-universe page has used since 2026-09-16), ~503 sequential `HISTORICAL_OPTIONS` calls at 1050ms spacing plus a retry pass (Yahoo daily-history fetch for realized vol happens inline per symbol, no separate pass, since it isn't Alpha-Vantage-rate-limited), reusing Sector Beeswarm's `meta.json` for company name/sector. Nav: added to the existing "Sentiment" dropdown (all 60 nav-carrying pages, scripted find/replace immediately after `/volatility.html`, verified afterward for exactly one insertion per file with zero duplicates/misses &mdash; including the one file, `volatility.html` itself, whose own nav link carries a `class="active"` attribute the plain-string script had to match separately), plus `index.html`'s homepage Research Areas grid in the matching "Sentiment &amp; Market Conditions" card. `node scripts/build-schedule-page.js` re-run afterward to regenerate `data-pull-schedule.html` (confirmed the new manual job listed with both real data sources auto-detected &mdash; `Alpha Vantage: HISTORICAL_OPTIONS` and `Yahoo Finance` &mdash; and its `relative-strength` read-dependency noted, no manual schedule-notes.json entry required beyond the one-line source note added for consistency with every other job's row). Tested by running the actual production background-job handler against a mocked `fetch` covering BOTH data sources (Alpha Vantage `HISTORICAL_OPTIONS` and Yahoo's chart endpoint) &mdash; a synthetic 35-company/11-sector universe with realized vol spread 15%&ndash;45% and ATM implied vol deterministically constructed as `realizedVol &times; 0.8 + 8pt` (a planted ~+8pt average VRP that shrinks toward zero, then negative, as realized vol rises &mdash; exercising both positive- and negative-VRP rendering) and 3-month relative return planted as a clean negative function of VRP, plus six dedicated edge-case tickers exercising every exclusion path separately: one whose every `HISTORICAL_OPTIONS` fetch attempt fails, one that returns an empty chain, one whose only expirations fall outside the 15&ndash;60 day band, one whose in-band expiration has no strike within 0.15 of a 0.50 delta, one with a good options chain but fewer than 21 Yahoo daily closes, and one with good options+price data but missing from the beeswarm sector metadata &mdash; plus deliberately-wrong-IV decoy contracts at an out-of-band expiration and off-ATM strikes, to confirm the selection logic actually picks the correct contract rather than trivially satisfying the test. Served through a local stub API with locally-vendored Chart.js/zoom-plugin (this sandbox's egress policy blocks the jsdelivr CDN the page loads them from in production, same workaround every prior session this week has documented), driven with Playwright across dark/light desktop and dark/light mobile (theme via `page.addInitScript`/`localStorage.theme`) plus the dependency-missing-warning-banner and "backend hasn't run yet" 502/empty-state scenarios. Backend assertions confirmed all six edge cases excluded correctly (35 of 41 synthetic tickers published: `excludedNoAtm`=3, `excludedNoRv`=1, plus FAILTIX failing every retry and NOSECTOR1 dropped at the metadata join), the expiration-band and delta-selection logic picked exactly the planted 30-day/0.50-delta contract every time (median days-to-expiry = 30 exactly, despite decoy contracts at a 90-day expiration and off-ATM strikes with deliberately wrong IV present in every mocked chain), and both planted correlations rendered in the expected directions (implied-vs-realized-vol pairs: Pearson r = 1.0000 by construction; VRP-vs-relative-return pairs: Pearson r = -1.0000 on the planted signal) &mdash; synthetic-data sanity checks confirming the selection logic, both regressions, and the histogram/leaderboard rendering all work correctly, not a claim about what the real S&amp;P 500 sweep will find once it runs post-merge. No bugs found — all Playwright checks (console/page errors, theme application, zero horizontal-overflow, stat/table/leaderboard population, sort-on-click, both dependency-missing and cold-start/502 states) passed on the first run.
* **Beta Stability** (2026-09-25, branch `claude/sharp-hopper-q7v9kg`, same branch/PR (#36) as the five pages above &mdash; sixth page of this multi-page night) &mdash; `/beta-stability.html`. Like the session that built Volatility Risk Premium, this session's assignment was an open-ended "find a genuinely fresh sixth idea or stand down" investigation against a backlog five successive sessions had already covered heavily. Two off-list candidates were considered before committing: (1) a beta-stability/rolling-beta-drift angle (the brief's own suggested angle, distinct from `/volatility.html`'s index-only VIX-vs-realized-vol comparison) and (2) a "quality of guidance"/estimate-accuracy angle distinct from `/analyst-price-target.html`, which on inspection sits close enough to Earnings Revisions' and Earnings Surprise's existing estimate-vs-actual machinery that it risked being a near-duplicate rather than a clearly separate question &mdash; not pursued further once (1) was confirmed feasible and clearly distinct. Confirmed via a real `TIME_SERIES_WEEKLY_ADJUSTED` test call that Alpha Vantage's weekly-adjusted endpoint returns full 20+ year history with no compact/outputsize option on this MCP tool (~100,000 characters for one ticker alone) &mdash; disproportionate for a ~503-ticker sweep, the same class of rejection Relative Strength Leaders/Laggards' own design note already flagged for daily full-history &mdash; so this page uses Yahoo Finance daily closes instead (`yahoo-client.js`'s `fetchDailyHistory`, the same already-proven-at-503-ticker-scale helper `/vol-risk-premium.html` and `/quality-lowvol-factors.html` use), making this **the first full-universe sweep on this site that needs zero Alpha Vantage calls**. **Core question:** is a stock's beta (market sensitivity vs. SPY) a durable property, or does it drift? `/equity-risk-premium.html` and `/roic-wacc.html` both read Alpha Vantage `OVERVIEW`'s `Beta` field &mdash; a single static number with an undisclosed methodology, never tested for stability. This page computes beta itself, twice, from two EQUAL, non-overlapping, back-to-back halves (older first, most recent last &mdash; the same "equal-length oldest-half vs. most-recent-half split" discipline as `/ai-capex-tracker.html`'s structural-break test, applied per company instead of to one aggregate series) of the trailing ~150 weekly log returns (about 2.9 years) common to both the stock and SPY, resampled from Yahoo daily closes to weekly (Monday-starting week, last trading day's close wins) rather than left as daily &mdash; daily returns over a multi-year window are heavily autocorrelated at the individual-stock level, which would overstate the precision of a beta estimated from them; weekly is the standard convention for a multi-year beta (Bloomberg's own default `BETA` function uses two years of weekly returns). **Drift** = recent-half beta &minus; older-half beta. A constituent with fewer than 104 weekly returns of common history (a recent IPO/spinoff, a halted/delisted name, or a Yahoo fetch failure surviving a retry pass) is excluded entirely, not compared on a shorter window. **Page:** sector-median-drift bar chart, a drift distribution histogram, a market-wide one-sample t-test + sign test on drift (same small-sample-honesty discipline as `/spin-off-performance.html`, here run at full S&amp;P-500 scale &mdash; has the average stock's beta shifted at all, in either direction?), the core cross-sectional test (recent-half beta vs. older-half beta, Pearson+Spearman &mdash; a direct persistence check: does last period's beta predict this period's?), a second scatter regressing drift against 3-month relative return (reusing `/relative-strength-leaders.html`'s blob, same optional-dependency/graceful-fallback pattern as `/roic-wacc.html`/`/rd-intensity.html`/`/vol-risk-premium.html`), biggest-increase/biggest-decrease/most-stable leaderboards, and a full sortable table (older beta, recent beta, drift, full-period beta, full-period R&sup2; as a rough per-stock fit-quality signal). **Backend:** new `scheduled-beta-stability-background.js`, one-time snapshot (no recurring schedule, matching the convention every full-universe page has used since 2026-09-16), ~504 sequential Yahoo fetches (503 constituents + SPY, SPY fetched once and reused for every constituent rather than once per constituent) at 300ms spacing with a retry pass &mdash; same cadence as `scheduled-quality-lowvol-background.js`'s own full-universe Yahoo sweep &mdash; reusing Sector Beeswarm's `meta.json` for company name/sector. Nav: added to the existing "Sentiment" dropdown (all ~61 nav-carrying pages, scripted find/replace immediately after `/vol-risk-premium.html`, verified afterward for exactly one insertion per file with zero duplicates/misses, including `vol-risk-premium.html` itself whose own link carries `class="active"`), plus `index.html`'s homepage Research Areas grid in the matching "Sentiment &amp; Market Conditions" card. `node scripts/build-schedule-page.js` re-run afterward to regenerate `data-pull-schedule.html` (confirmed the new manual job listed with `Yahoo Finance` as its only data source and its `relative-strength` read-dependency noted). **Testing:** ran the actual production background-job handler against a mocked Yahoo `fetch` and a mocked Netlify Blobs store (via `Module._cache` substitution of the real `@netlify/blobs` package and of `breadth-constituents.js`, rather than editing any file under `netlify/functions/`) &mdash; a synthetic 34-company/9-usable-sector universe (2 sectors with only 2 names each were deliberately left below the page's own `MIN_SECTOR_N=3` to exercise that exclusion too) with per-ticker planted older-half/recent-half betas spanning a wide range, plus four dedicated edge-case tickers: one whose every Yahoo fetch attempt fails (survives both passes, absent from the final blob), one with only ~1 year of daily history (below the 104-week common-history minimum, excluded), one with valid price data but deliberately dropped from the beeswarm sector metadata fixture (excluded at the join), and one with a literally flat price for its entire history (zero variance throughout, exercising the null-R&sup2;-but-still-published code path rather than a crash). Run twice (relative-strength dependency populated, then absent) to exercise both branches of that optional read. **Getting a clean planted signal took two real corrections, both logged directly in code comments so a future session doesn't have to re-derive them:** (1) an initial 1,100-weekday synthetic history with the beta-regime switch planted at its literal midpoint silently misaligned with the job's own logic, which trims each stock's weekly series to the most recent ~151 weeks *before* splitting &mdash; a debug script (`debug-align.js`) confirmed the split-point *dates* actually matched exactly once the fixture's total history was shortened to ~152 weekdays (barely more than the job's own window), ruling out an alignment bug; (2) at full realistic idiosyncratic daily volatility, ~75 weekly return samples per half is too few to reliably recover a planted beta difference above the noise (confirmed with a standalone tuning script, `tune-noise.js`, which showed several planted cases coming back wrong-signed at full noise and consistently correctly-signed at 12% of it) &mdash; idiosyncratic vol was scaled to 12% of its original (still-labeled, realistic-looking) per-ticker value for the fixture only, a test-data tuning choice, not a change to any production code. With both fixes applied: the persistence regression (recent-half beta vs. older-half beta) rendered r&asymp;0.89, the drift-vs-relative-return regression r&asymp;-0.90 on the planted signal, the single flat-price edge case correctly showed drift=0/R&sup2;=null and appeared correctly in the "most stable" leaderboard, and the short-history/fetch-failure/no-sector edge cases were each excluded via the correct distinct path (confirmed via the job's own log line and the blob's `excludedShortHistory`/`loadedCount` counts) &mdash; synthetic-data sanity checks confirming the mechanism and every exclusion path work correctly, not a claim about what the real S&amp;P 500 sweep will find once it runs post-merge. Served through a local stub API with locally-vendored Chart.js/zoom-plugin (this sandbox's egress policy blocks the jsdelivr CDN the page loads them from in production, same workaround every prior session this week has documented), driven with Playwright across dark/light desktop and dark/light mobile (theme via `page.addInitScript`/`localStorage.theme`) plus the dependency-missing-warning-banner and "backend hasn't run yet" 502/empty-state scenarios. One test-harness-only false positive was caught and fixed during this pass (not a page bug): the initial Playwright assertion flagged Chrome's own network-layer console log of the mocked 502 response as a "console error" on the intentional cold-start scenario; refined to exclude that expected line rather than loosen the check generally. No actual page bugs found &mdash; every other check (console/page errors, theme application, zero horizontal-overflow, stat/table/leaderboard population, sort-on-click, both dependency-missing and cold-start/502 states) passed.

Already attempted, skipped
(Update at the end of every run with why, so future runs don't re-try the same dead end)

* **Workforce Productivity — Revenue per Employee** (2026-09-25, session that built `/52-week-high-momentum.html` above) — assigned as this session's third page, built instead as `/52-week-high-momentum.html` once this turned out to be a confirmed dead end, not attempted-and-abandoned. The idea depended on Alpha Vantage `COMPANY_OVERVIEW`'s documented `FullTimeEmployees` field for current headcount. Real test calls against five different, large, well-covered S&P 500 constituents (AAPL, IBM, WMT, NVDA, CRWD — spanning tech, industrials, retail, semis, and a smaller-cap SaaS name, to rule out a single-company data gap) returned a complete `COMPANY_OVERVIEW` payload with no `FullTimeEmployees` field or any other employee-count field anywhere in the response, and a direct check of the endpoint's own tool schema (`TOOL_GET`) confirmed no optional parameter unlocks it either. This is **not** the kind of OVERVIEW field-naming drift this site has hit before (e.g. `PE Divergence`'s methodology notes on OVERVIEW field quirks) — it's a genuine absence on this account's entitlement, confirmed across a diverse sample, not a name to go hunting for. SEC EDGAR's XBRL company-facts API (`data.sec.gov`, the standard free alternative for per-company headcount via the `dei:EntityNumberOfEmployees` tag, joinable off the `CIK` field `COMPANY_OVERVIEW` already returns) was identified as a plausible substitute data source, but this sandbox's network egress policy blocks `data.sec.gov` outright (confirmed via both a direct `curl` through the proxy and the `WebFetch` tool, both returning `EGRESS_BLOCKED`), so its exact field names/response shape could not be verified with a real test call the way this codebase's own conventions require before shipping a scheduled job against it. **Manual decision needed / worth a future look**: someone running a session with unrestricted network access could verify SEC EDGAR's `companyconcept` endpoint (`dei:EntityNumberOfEmployees` and a revenue tag such as `us-gaap:RevenueFromContractWithCustomerExcludingAssessedTax`, joined by CIK) as a genuine substitute data source for this exact page concept (revenue-per-employee, operating-leverage growth-gap framing) — the analytical design in the original assignment is sound, only the data source was the blocker. Until then, treat this as a confirmed dead end on Alpha Vantage specifically, not a "not yet attempted" gap.
* **Earnings Surprise History (beat/miss rates by sector)** and **Dividend Growth Screener** — both built independently in *this* session (Alpha Vantage `EARNINGS` and `DIVIDENDS` respectively), tested, and initially shipped as `nightly/2026-09-13-earnings-surprise` / part of `nightly/2026-09-13-dividend-growth` — then removed and dropped from the stack once it turned out a separate, concurrently-running session had already built both of these same two ideas first, unmerged, in PR #4 (`claude/sharp-hopper-t1gvy2`, created 2026-09-13 10:14 UTC — well before this session started, but never merged to `main`, so this session's `ROUTINE_BRIEF.md` read at start-of-run had no way to know). Real duplicate work, not a design flaw in either implementation. **Status as of 2026-09-16: PR #4 has been rebased onto current `main` and merged.** Both pages are now live (`/earnings-surprise.html`, `/dividend-growth-screener.html` — see the "Already built" entries above) — no further action needed, and this note is kept only as the historical record of the duplicate-work incident.
* **New Highs/New Lows Ratio** — skipped, not a data gap. `/market-breadth.html` already has a "New Highs − New Lows" stat card and a dedicated "52-week new highs vs. new lows" chart; a separate page would be a near-duplicate.
* **Equity Factor Performance (value/growth/momentum/quality/low-vol)** — skipped, not a data gap. `/factor-analysis.html` already covers Market/Size(SMB)/Value(HML)/Momentum via the Ken French Data Library back to 1926; a separate page would mostly re-plot the same series. (Quality and low-vol specifically aren't covered — could be a future addition to that existing page rather than a new one, if picked up later.)
* **IPO Aftermarket Performance** — a true historical version (join Ritter's `/ipo-activity.html` data against price history to compute actual post-IPO returns) remains not buildable: Ritter's dataset is aggregate year-level stats only, no individual IPO ticker list, confirmed by PR #13's investigation and re-confirmed this session. **Partially superseded**: this session's new IPO Pipeline & Aftermarket Tracker (`/ipo-pipeline.html`, see "Already built" above) covers the forward-looking version of this idea instead — don't rebuild a historical-backfill attempt, it's a confirmed dead end; if picked up again, the angle worth exploring is a different free historical source with a real per-IPO ticker list (none found in two separate sessions' searches so far).
* **Short Interest Tracker** (2026-09-15) — investigated and skipped, not built. FINRA's old anonymous `equityShortInterest` data endpoint was deprecated in April 2021; its replacement now lives behind FINRA's API Console and requires a registered FINRA API Console account with OAuth client credentials (confirmed via web research this run). That registration isn't configured in this environment and can't be self-served headlessly — it needs a human to sign up for a FINRA API Console account, create an OAuth client, and add the resulting credentials as a Netlify env var before this idea is buildable. **Manual decision needed**: someone with FINRA API Console access has to do that registration; until then, leave this skipped rather than re-investigating the same dead end.
* **Retail vs. Institutional Flow** (2026-09-21) — investigated and skipped, not built. This is the third open "coming soon" placeholder under Ownership & Flows (Options Positioning has since been filled this run — see "Already built" above). No free, public, per-stock data source separating retail from institutional trading volume exists: general market-data APIs (Alpha Vantage, FMP, EODHD, Finnhub) expose price/volume aggregates and fundamentals, not that segmentation, and the providers that do (Databento and similar institutional-infrastructure vendors) are paid. Same category of dead end as Short Interest Tracker below (proprietary data, no free tier covers it) rather than a "not yet attempted" gap — don't re-investigate without a genuinely new candidate source in hand.
* **Buyback Announcements** (dollar-value announced-buyback-program tracking, distinct from the share-count-based buyback signal already covered by `/share-count-trends.html`, and distinct from `/buyback-effectiveness.html`'s spend-vs-share-count-outcome angle — see the tracking-list catch-up entry above) — no Alpha Vantage endpoint. Re-investigated 2026-09-21 in a session with normal (not sandbox-restricted) `WebSearch` access, to follow up on the earlier note that this was "not written off, just not attempted" under network restriction. Still no viable free, headless, maintained source: the specialized options that showed up (an Apify "Stock Buyback Announcement Tracker" actor, an "Alphanume" buyback dataset) are third-party paid/unclear-licensing products, not free public data like FRED/BLS/Treasury/an exchange site; general market-data APIs (FMP, Finnhub, EODHD, Marketstack) don't expose buyback-authorization-amount data on their free tiers; "The Online Investor" has a buybacks tracker but it's a website to scrape, not an API, and scraping a third-party site's proprietary tracker isn't the kind of free public source this brief's sourcing rules point at. Treat this as a firmer dead end now (two independent look, same conclusion) rather than "worth a look" — don't re-investigate again without a genuinely new candidate source in hand. (**Spin-off Performance Tracker**, investigated alongside this at the time, has since been built — see the "Already built" entry above; it turned out real Alpha Vantage price data plus a small hand-compiled event list was sufficient, no separate spin-off-specific data source was actually needed.)
* **Ownership concentration / 13F crowding (Herfindahl-Hirschman Index or top-10-holder %)** (2026-09-25) — investigated as a candidate fifth page (the brief's own suggested angle: "ownership concentration... beyond what `/institutional-ownership.html` already does"), skipped on cost/reliability grounds, not a data-availability dead end. `INSTITUTIONAL_HOLDINGS`'s per-holder `holdings` array is real and already fetched by `/institutional-ownership.html`'s own backend — that page's methodology text says outright it "doesn't use it" because it's "thousands of rows for a mega-cap." A real test call against AAPL confirmed exactly why: the endpoint has no size limit or pagination parameter and returns every one of AAPL's 6,491 real institutional holders in a single response (459,139 tokens via the full-data path) — no way to request only the top N. A full ~503-call sweep would carry a per-call payload that scales with how widely-held each name is (small for thin-coverage names, huge for mega-caps), a materially different and less predictable cost profile than any other sweep on this site, with real risk of a Background Function's ~15-minute ceiling being blown on the market's most widely-held constituents specifically — the names a concentration page would most want to cover well. **Worth a future look**: a narrower version that reads only the first N holders as the API returns them (accepting an approximate, not a true, HHI — a real limitation to flag explicitly if built) was not attempted this run; if picked up, that's the angle to try rather than the full per-holder list.
* **Capital structure / convertible or preferred stock exposure** (2026-09-25) — investigated as a candidate fifth page, confirmed a firm data-availability dead end. A real `BALANCE_SHEET` test call against Bank of America (a large, likely preferred-stock issuer, chosen specifically to give the field its best chance of appearing) returned a complete balance sheet with no preferred-stock field anywhere in the response — no `preferredStockValue`, no shares-outstanding equivalent, nothing — not even reported as `"None"` the way other genuinely-tracked-but-empty fields on this endpoint are. Alpha Vantage's fundamentals endpoints (`OVERVIEW`, `BALANCE_SHEET`, `INCOME_STATEMENT`, `CASH_FLOW`) have no convertible-debt or preferred-equity line items at all. Same category of dead end as Revenue-per-Employee's `FullTimeEmployees` gap above — a genuine absence on this account's entitlement, not a field-naming quirk to go hunting for.
