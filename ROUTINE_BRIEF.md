Nightly Routine Brief — michaeljstevenson.co
## ⚠️ DO NOT STOP AFTER ONE PAGE
This is the single most important rule in this file, and it has been missed
before: after finishing a page — including after it's tested, committed,
pushed, and its PR is green and mergeable — **do not stop and do not consider
the run "done."** Immediately move to the next backlog item (or propose a new
one) and keep building. Only stop when the safety valve below is hit
(~85% of the session window) or the idea backlog is genuinely exhausted.
"I built one good page" is never a finishing condition on its own.
Site context
Independent markets-research portfolio site (data-driven research on markets, macro, and investor psychology). Existing sections: Behavioral/Positioning, Equities, Markets/Macro, Rates/Credit, Alternative/Cross-Asset, Thematic. Tone: professional, data-first, "not investment advice."
Nightly task
Build fully polished equities-research pages — not stubs. The backlog below is a source of ideas, not a restriction: use it as a starting point, but feel free to propose and build other equities-related page ideas that fit the site's style, whether or not they're on the list. No cap on how many per night: build as many as can be finished to the "fully polished" standard below within the run. Never sacrifice polish/data depth just to bump the count — a single well-built page beats several half-finished ones.
Model

* Default to Sonnet 5, effort level: low for standard page-building work (scaffolding, layout, wiring up already-clear data pulls).
* Reserve Sonnet 5, effort level: medium for the harder analytical work — designing the statistical methodology, deciding how to structure a non-obvious dataset, or reasoning through what a metric actually means. Switch up only when the task genuinely calls for it, not by default.

Safety valve
Stop the run once it has consumed ~85% of the current 5-hour session window (Pro plan), even if the backlog isn't exhausted and even if fewer than 3 pages are done. Finish the page in progress to a clean stopping point rather than abandoning it mid-file, then wrap up and open the PR with whatever is complete.
Avoiding repeated/redundant work across runs

* Maintain a "Already built" list at the bottom of this brief file (see section below) and update it at the end of every run. Never re-derive or re-attempt a page already on that list.
* In the PR description, log what was skipped and why (e.g. "IPO Pipeline skipped — no free data source found with sufficient history after exhausting Alpha Vantage + web search") so the next run doesn't re-investigate the same dead end. Add skipped items to the "Already attempted, skipped" list below too.

Core objective: analysis first, then presentation
The real value of this site is turning complex equities/markets datasets into something a reader can actually understand — not just displaying numbers. For every page:

1. Analyze first. Do the actual statistical/quantitative work on the data — don't just plot a raw series. Reference [/factor-analysis](https://michaeljstevenson.co/factor-analysis) as the standard: it runs real statistical tests (correlation, Chow tests for structural breaks, robustness checks across different sample splits), and is transparent about methodology choices and even past mistakes made while building it.
2. Then visualize/present. Turn that analysis into a clean, friendly presentation — charts people can read at a glance, plain-language explanations of what the data means and why it matters, not just jargon or a raw chart dump. Depth of analysis matters more than page count — see Safety valve above.

Scope: Equities section only
This routine is restricted to building out the Equities section of the site. Do not build Markets/Macro, Rates/Credit, Alternative/Cross-Asset, or Behavioral/Positioning pages, even if they're listed as "coming soon" elsewhere on the site.
Idea backlog (starting points — not exhaustive, not required in order)

* Earnings Revisions
* Insider Buying/Selling
* Short Interest Tracker
* Buyback Announcements
* IPO Aftermarket Performance
* Dividend Growth Screener
* Small Cap vs. Large Cap Spread
* International vs. US Relative Performance
* Sector Rotation Model
* Analyst Estimate Dispersion
* Earnings Surprise History (beat/miss rates by sector)
* Buyback Yield vs. Dividend Yield
* New Highs/New Lows Ratio
* Equity Risk Premium by Sector
* Index Concentration (top 10 weight vs. history)
* Relative Strength Leaders/Laggards
* IPO Pipeline / Filing Tracker
* Share Count Trends (dilution vs. buybacks)
* Forward P/E vs. Trailing P/E Divergence
* Earnings Growth vs. Price Performance Divergence
* Sector Correlation Matrix
* Small Cap Liquidity/Volume Trends
* Stock Split Tracker
* Spin-off Performance Tracker
* Equity Factor Performance (value/growth/momentum/quality/low-vol)

Data sourcing (required)

1. Use the Alpha Vantage API (key already configured) as the first choice for any data the page needs — equities, FX, commodities, macro indicators, technicals, etc. Check what Alpha Vantage covers before assuming it can't.
2. If Alpha Vantage doesn't have sufficient depth for a given metric (e.g. missing series, insufficient history, no direct dataset), search free public sources online instead — e.g. FRED, BLS, Treasury.gov, World Bank, ECB/ONS/other central bank data portals, CFTC, exchange sites, or other free datasets relevant to the topic.
3. Do not give up and ship a page with placeholder, mocked, or illustrative-only data. If depth is genuinely unavailable after exhausting Alpha Vantage and a real search for free alternatives, scale the page's scope down (fewer charts/metrics) rather than fabricate data — but exhaust the search first.
4. Note the actual data source(s) used for each chart/metric in the page's methodology blurb and in the PR description.

"Fully polished" means

* [ ] Matches existing page layout/style conventions (check an existing page like /sector-analysis or /volatility as the template)
* [ ] Real data wired up (not placeholder text/numbers) where feasible via available APIs
* [ ] Chart(s)/visualization consistent with the site's existing chart style
* [ ] Short methodology/explainer blurb, matching the site's tone
* [ ] Added to the correct nav section, "coming soon" tag removed if applicable
* [ ] Responsive / doesn't break mobile layout

Boundaries — do not touch

* Only build pages under the Equities section (see Scope above) — do not create or modify pages in any other section
* Do not modify any already-live research page (Sentiment Index, Volatility, Sector Performance, etc.) unless explicitly asked
* Do not change deploy config, site-wide nav structure, or global styles
* Do not push to `main` — open a PR against a new branch per run

Output

* One PR per night, branch name `nightly/<date>-<short-topic>`
* PR description: what was built, what data source was used, what was skipped and why, anything that needs a manual decision (e.g. data source choice, ambiguous methodology)

Already built
(Update this list at the end of every run — do not re-attempt these)

* **Earnings Revisions** (2026-09-13) — analyst EPS estimate revision breadth (Net Revision Ratio) and magnitude (Estimate Drift) across the S&P 500, by sector, with a historical trend, breadth-vs-magnitude scatter, and upgrade/downgrade leaderboards. Data: Alpha Vantage `EARNINGS_ESTIMATES` (weekly full-universe sweep), sector/name metadata reused from the Sector Beeswarm page's own weekly refresh. See `netlify/functions/scheduled-revisions-background.js` for full methodology notes.
* **Index Concentration (top 10 weight vs. history)** — already live as `/concentration.html` (built prior to this routine's existence; noting it here so a future run doesn't re-derive it from the backlog).
* **Insider Buying/Selling** (2026-09-13, `nightly/2026-09-13-insider-buying-selling`) — `/insider-buying-selling.html`. Officer/director/10%+-owner Form 4 activity (Alpha Vantage `INSIDER_TRANSACTIONS`) across the full S&P 500, trailing 90 days: net $ buy/sell flow by sector, cluster-buying screen (2+ insiders buying, zero selling), largest individual purchases/sales, full sortable per-stock table. Backend: weekly `scheduled-insider-transactions-background.js` (Saturday 10:30 UTC) → Netlify Blobs (`insider-transactions` store) → `insider-transactions.js`, reusing the Sector Beeswarm page's `meta.json` for company name/sector rather than a second OVERVIEW sweep. See that run's PR description for the full write-up, including the signal-quality filtering rationale (Common-Stock-only, priced-only transactions) and its limits.
* **Small Cap vs. Large Cap Spread** (2026-09-13, `nightly/2026-09-13-smallcap-vs-largecap`) — `/small-cap-vs-large-cap.html`. Relative strength of IWM (small-cap) and MDY (mid-cap) vs. SPY (large-cap), indexed since IWM's May 2000 inception: long-run ratio chart, trailing-return ladder (1M–10Y, cumulative under 1Y / annualized 3Y+), rolling 12-month relative-return oscillator, average monthly spread return by Fed-funds-rate regime (Hiking/Holding/Cutting, classified off trailing 3-month change), and a Pearson+Spearman regression of monthly spread return against the month's change in the 10-year Treasury yield (methodology matches `/factor-analysis`'s two-method check). Backend: daily `scheduled-smallcap-background.js` (weekdays 21:15 UTC — only 5 Alpha Vantage calls, no heavy pacing needed) → Netlify Blobs (`smallcap-largecap` store) → `smallcap-largecap.js`. Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` (IWM/MDY/SPY, full history) + `FEDERAL_FUNDS_RATE` + `TREASURY_YIELD` (10-year, both monthly, full history). Tested locally with a synthetic-but-realistic dataset (real Fed-funds/Treasury history, simulated price paths) served through a local stub API and driven with Playwright (dark/light/mobile) — caught and fixed two bugs pre-merge: floating-point-artifact y-axis tick labels on the regime bar chart, and a non-pluralized "1th percentile" ordinal.
* **Share Count Trends (dilution vs. buybacks)** (2026-09-13, `nightly/2026-09-13-share-count-trends`, stacked on top of the Small Cap vs. Large Cap Spread branch/PR above — merge that one first, or merge this PR as-is since it carries both diffs) — `/share-count-trends.html`. Reads buybacks vs. dilution directly off quarterly `commonStockSharesOutstanding` (Alpha Vantage `BALANCE_SHEET`) at 1/3/5-year lookbacks across the full S&P 500 — deliberately not sourced from separate buyback-announcement or dollar-value data, since the share count itself already nets out every buyback, issuance, and stock-based-comp dilution. Sector aggregates, a year-over-year persistence check (Pearson+Spearman, same non-overlapping-annual construction as `/factor-analysis`'s momentum test — does last year's buyback pace predict this year's?), buyback/dilution streak leaderboards, and a full sortable company table. Backend: weekly `scheduled-share-count-background.js` (Saturday 10:50 UTC, after the insider-transactions sweep), reusing Sector Beeswarm's `meta.json` for company name/sector. Tested locally the same way as this session's other pages (synthetic dataset with a deliberately-correlated year-over-year buyback signal, served through a local stub API, driven with Playwright in dark/light/mobile) — no bugs found.
* **Sector Correlation Matrix** (2026-09-13, `nightly/2026-09-13-dividend-growth`, stacked on top of the Share Count Trends branch/PR above — merge that one first, or merge this PR as-is since it carries both diffs) — `/sector-correlation.html`. Full pairwise Pearson correlation matrix (heatmap) across the 11 SPDR sector ETFs, a rolling 63-day average-correlation time series, and a direct test of the "correlations go to 1 in a crisis" claim (rolling avg. correlation regressed against rolling SPY realized volatility, Pearson+Spearman). Needed zero new Alpha Vantage calls or backend function — it's pure client-side analysis of the daily price history the existing `/api/sector-performance` endpoint (behind Sector Performance) already fetches and serves. Tested locally with a synthetic dataset that includes a deliberate "stress regime" (elevated common-factor weight over one stretch) to confirm the vol/correlation relationship renders correctly (r ≈ +0.98 in the synthetic case, as designed). No bugs found.
* **Sector Rotation Model** (2026-09-13, `nightly/2026-09-13-sector-rotation`, stacked on top of the Sector Correlation Matrix branch/PR above — merge earlier PRs first or merge this one as-is) — `/sector-rotation.html`. A Relative Rotation Graph (RRG)-style view of the 11 SPDR sector ETFs: current level (3-month relative-strength change) vs. momentum (1-month relative-strength change), classified into the standard Leading/Weakening/Lagging/Improving quadrants; a grouped-bar view of relative return across 1M/3M/6M/1Y; and a month-over-month rank-persistence test (Spearman) asking whether chasing last month's sector leaders actually works. Also reuses `/api/sector-performance` with no new Alpha Vantage calls. Tested locally with a synthetic dataset; found and fixed a real bug during testing — a sector label near the RRG chart's right edge (a strong "Leading" sector, the most interesting case) drew off-canvas on narrow viewports, now flips to the point's left when there isn't room on the right.
* Considered but skipped as near-duplicates of existing pages (see "Already attempted, skipped" below): **New Highs/New Lows Ratio** (already substantially covered by Market Breadth's "52-week new highs vs. new lows" chart) and **Equity Factor Performance** (already substantially covered by Factor Analysis's Market/Size/Value/Momentum Fama-French series).
* **Earnings Surprise History** (2026-09-13) — `/earnings-surprise.html`. Beat/meet/miss rates across the full S&P 500 (Alpha Vantage `EARNINGS`, which returns real multi-year quarterly history per call — unlike `EARNINGS_ESTIMATES`, no accumulating snapshot needed): beat-rate trend over the last ~12 calendarized quarters, sector breakdown for the latest well-covered quarter (beat rate and avg. surprise %, toggle between the two), surprise-magnitude distribution histogram, a pooled lag-1 "surprise persistence" test (beat-rate-after-a-beat vs. beat-rate-after-a-miss, two-proportion z-test with p-value) as the page's real-stats component, plus longest active beat/miss streak leaderboards and biggest-beat/miss-this-quarter tables. Backend: weekly `scheduled-surprise-background.js` (Saturday 11:10 UTC, after the rest of the Saturday sweep block) → Netlify Blobs (`earnings-surprise` store) → `earnings-surprise.js`, reusing Sector Beeswarm's `meta.json` for sector/name. Filters out quarters with a consensus estimate under 5¢ or a surprise beyond ±200% (divide-by-near-zero blowups, e.g. a real JPM 2009 print of a 367% "surprise") before they can distort a mean or leaderboard — see the file's own comments and the page's methodology blurb for the full rationale.
* **Dividend Growth Screener** (2026-09-13) — `/dividend-growth-screener.html`. Alpha Vantage `DIVIDENDS` (full historical ex-dividend record per call, going back decades for long-time payers) summed into calendar-year totals per S&P 500 constituent — frequency-agnostic, so it works the same for quarterly, semi-annual, or annual payers. Computes, per company: current consecutive-year growth streak (Dividend-Aristocrat-style, no claim about the formal 25-year threshold), 5-year dividend CAGR, and latest YoY growth. Page shows: market-wide median dividend growth trend over the last ~10 years, sector breakdown (avg. 5Y CAGR / avg. streak, toggle), YoY growth distribution histogram, and leaderboards for fastest growers, longest active streaks, and recent cutters. Backend: weekly `scheduled-dividend-growth-background.js` (Saturday 12:00 UTC, after `scheduled-surprise-background`) → Netlify Blobs (`dividend-growth` store) → `dividend-growth.js`, reusing Sector Beeswarm's `meta.json`. Non-payers and payers with under 3 full calendar years of history are excluded entirely — this page only screens established payers, so expect universe coverage well under the full ~500 (most of the S&P doesn't pay a dividend, or hasn't for 3+ years).
* **International vs. US Relative Performance** (2026-09-14, `nightly/2026-09-14-international-vs-us`) — `/international-vs-us.html`. Relative strength of developed-ex-US (EFA) and emerging-markets (EEM) equities against the US (SPY), indexed since EEM's April 2003 inception: long-run ratio chart, trailing-return ladder, rolling 12-month relative-return oscillator, average monthly spread return by dollar regime (Dollar Strengthening/Flat/Weakening, classified off the trailing 3-month change in EUR/USD), and a Pearson+Spearman regression of monthly relative return against the month's EUR/USD change — architecture directly mirrors Small Cap vs. Large Cap Spread. Backend: daily `scheduled-international-background.js` (weekdays 21:20 UTC — only 4 Alpha Vantage calls, no heavy pacing needed) → Netlify Blobs (`international-us` store) → `international-us.js`. Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` (EFA/EEM/SPY, full history) + `FX_MONTHLY` (EUR/USD). Tested locally with a synthetic dataset (designed positive EUR/USD-vs-relative-return correlation) served through a local stub API, driven with Playwright (dark/light/mobile) — no bugs found.
* **Analyst Estimate Dispersion** (2026-09-14, `nightly/2026-09-14-international-vs-us`, stacked on top of the International vs. US Relative Performance commit above) — `/analyst-estimate-dispersion.html`. How much analysts disagree with each other about a stock's current-fiscal-year EPS (relative high/low range around the consensus average, from the same `EARNINGS_ESTIMATES` payload Earnings Revisions uses) — a distinct question from whether the consensus itself is moving. Sector-by-sector median dispersion, a distribution histogram across the S&P 500, a market-median history that accumulates weekly, a Pearson+Spearman test of whether dispersion coincides with more analyst revision activity, and most-disagreement/most-consensus leaderboards (min. 5 covering analysts). Backend: new weekly `scheduled-dispersion-background.js` (Saturday 11:10 UTC, after Share Count Trends' sweep) → Netlify Blobs (`analyst-dispersion` store) → `dispersion.js` — a standalone full-S&P-500 `EARNINGS_ESTIMATES` sweep kept separate from `scheduled-revisions-background.js` (Earnings Revisions' own job) so this new page doesn't touch that already-shipped page's backend, at the cost of a second, otherwise-redundant sweep of the same endpoint. Reuses Sector Beeswarm's `meta.json` for company name/sector. Tested locally with a synthetic dataset (designed sector-level dispersion spread and a positive dispersion-vs-revision-churn correlation), driven with Playwright (dark/light/mobile) — no bugs found.
* **Shareholder Yield: Buybacks vs. Dividends** (2026-09-14, `nightly/2026-09-14-international-vs-us`, stacked on top of the Analyst Estimate Dispersion commit above) — `/shareholder-yield.html`. Combines dividend yield and buyback yield into one "shareholder yield" number across the S&P 500 — the backlog's "Buyback Yield vs. Dividend Yield" idea — plus a Pearson+Spearman test of the common claim that dividend-payers and buyback-heavy companies are different populations (mature/stable cash flow → dividends; more variable earnings → discretionary buybacks). Stacked-bar sector view, a dividend-yield-vs-buyback-yield scatter with regression, and three leaderboards (highest combined, highest dividend-only, highest buyback-only). Backend: new weekly `scheduled-shareholder-yield-background.js` (Saturday 11:25 UTC, after both Share Count Trends' and Analyst Estimate Dispersion's sweeps) → Netlify Blobs (`shareholder-yield` store) → `shareholder-yield.js`. Dividend yield comes from a dedicated `COMPANY_OVERVIEW` sweep (its `DividendYield` field is already trailing and price-normalized — no separate price series needed). Buyback yield is deliberately *not* re-derived from `BALANCE_SHEET`: it's read straight off Share Count Trends' already-computed trailing-12-month share-count change (negated), reusing that page's existing weekly output instead of a second, redundant `BALANCE_SHEET` sweep of the same data — the one place this session's three new pages read (not just metadata-join) another page's own computed output rather than raw Alpha Vantage data. Tested locally with a synthetic dataset (designed mild negative dividend/buyback correlation to exercise the "substitution" story), driven with Playwright (dark/light/mobile) — no bugs found.
* **Stock Split Tracker** (2026-09-14, this session) — `/stock-split-tracker.html`. Split activity across the full S&P 500: a 15-year frequency history (forward vs. reverse splits — is the split coming back after two quiet decades?), sector activity in the trailing 5 years, and an event study of relative price performance vs. SPY in the trading days around each recent forward split (a Pearson+Spearman regression of split ratio size against 120-day post-split relative return). Data: Alpha Vantage `SPLITS` (full-universe sweep) + `TIME_SERIES_DAILY_ADJUSTED` (only for the smaller subset of tickers with a split in the last 5 years). Found and filtered a real data-quality issue: `SPLITS` mixes in non-split spin-off adjustment factors (GE's 2023/2024 spin-off ratios showed up as fake "splits") — see `netlify/functions/scheduled-splits-background.js` for the ratio-allowlist filter this required. Backend: weekly `scheduled-splits-background.js` (Saturday 12:20 UTC — moved from its original 11:10 UTC after a same-minute collision with `scheduled-surprise-background`, from a concurrently-built PR, was caught and fixed while merging that PR into `main`).
* **Forward vs. Trailing P/E Divergence** (2026-09-14, this session) — `/pe-divergence.html`. The market's own implied year-ahead EPS growth rate, read directly off `TrailingPE / ForwardPE - 1` (both ratios share the same price numerator, so no separate EPS-estimate data is needed) — sector medians, a distribution histogram, an accumulating weekly history (Alpha Vantage `OVERVIEW` is a snapshot, so this builds up real history one run at a time, same pattern as Earnings Revisions), and a Pearson+Spearman regression testing whether last quarter's actual EPS growth predicts the market's implied forward growth. Data: Alpha Vantage `OVERVIEW` (full-universe sweep, its own standalone pass rather than reusing Sector Beeswarm's weekly `OVERVIEW` sweep — see that page's methodology section for why). Backend: weekly `scheduled-pe-divergence-background.js` (Saturday 12:45 UTC — moved from its original 11:40 UTC for the same reason as Stock Split Tracker's schedule note above).
* **Relative Strength Leaders/Laggards** (2026-09-14, branch `claude/bold-clarke-oi26nk`) — `/relative-strength-leaders.html`. 1-month and 3-month price momentum relative to SPY across the full S&P 500: sector-aggregate bar chart, Leader/Neutral/Laggard tagging (top/bottom quintile of 3-month relative return), paired Leaders/Laggards tables, a full sortable per-stock table, and a forward-return persistence test (Pearson+Spearman) — deliberately built as "this week's rank vs. *next* snapshot's realized forward return," not "this week's rank vs. next week's rank," since two 63-trading-day windows one week apart share 58 days and would correlate almost mechanically regardless of real momentum (the same overlapping-window pitfall Share Count Trends' methodology calls out). Data: Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED`, **compact outputsize** (not full) for all ~504 calls (503 constituents + SPY) — full-history outputsize was tested directly against Alpha Vantage during design (confirmed AAPL alone returns 6,756+ daily bars, ~580K tokens) and rejected as disproportionate to what a 21/63-trading-day lookback needs; this is a deliberate scope-down from a 1Y/6M ladder (like Small Cap vs. Large Cap Spread's) in favor of keeping the sweep the same cost/reliability class as the site's other ~503-call weekly jobs. Backend: new weekly `scheduled-relative-strength-background.js` (Saturday 13:10 UTC — moved from its original 12:00 UTC after a same-minute collision with `scheduled-dividend-growth-background`, from a concurrently-built PR, was caught and fixed while merging this PR into `main`; now runs after `scheduled-pe-divergence-background` at 12:45), reusing Sector Beeswarm's `meta.json` for company name/sector. Weekly snapshots (up to 20 retained) accumulate in a `history.json` blob for the persistence test — like Earnings Revisions and PE Divergence, it needs a week or two of real runs before that test has enough data, and shows a graceful "not enough history yet" message until then. Tested locally with a synthetic 503-company dataset (Playwright, dark/light/mobile, plus a deliberately-correlated persistence signal to confirm the forward-return test computes and renders correctly) through a local stub API and locally-vendored Chart.js (this sandbox's egress policy blocks the jsdelivr CDN the page loads Chart.js from in production, same issue noted by the prior session that built Stock Split Tracker/PE Divergence) — no bugs found.
* **Equity Risk Premium by Sector** (2026-09-14, branch `claude/bold-clarke-oi26nk`, same branch/PR as Relative Strength Leaders/Laggards above) — `/equity-risk-premium.html`. A simple "Fed model" equity risk premium proxy (trailing earnings yield − 10-year Treasury yield) across the full S&P 500 and its 11 GICS sectors — deliberately much cruder than `/implied-erp` (Damodaran's FCFE/DCF model, sourced not computed), and the methodology section says so explicitly rather than implying it's a competing "true" ERP number; chosen specifically because it's computable per-sector and per-stock from data Alpha Vantage already has, which the Damodaran number isn't. Sector median-ERP bar chart, a weekly-accumulating market-median-ERP trend line, Highest/Lowest-ERP leaderboards, a full sortable table, and a single-snapshot cross-sectional test (no cold-start wait, unlike the forward-return tests on other new pages this run) of whether the market prices risk rationally — regressing each stock's ERP against its Alpha Vantage-reported beta, Pearson+Spearman. Data: Alpha Vantage `COMPANY_OVERVIEW` (full ~503-ticker sweep for trailing P/E and beta) + one `TREASURY_YIELD` call. Backend: new weekly `scheduled-equity-risk-premium-background.js` (Saturday 13:35 UTC — moved from its original 12:30 UTC, which landed mid-window of `scheduled-splits-background`'s sweep from a separate, concurrently-built PR; now runs after `scheduled-relative-strength-background` at 13:10, once that job's own unrelated schedule collision was fixed too — see both functions' own schedule comments). Deliberately reads name/sector off each stock's own `COMPANY_OVERVIEW` response (same `normalizeSector` GICS-normalization helper Sector Beeswarm's own metadata job uses) rather than joining against another page's blob, since this job already pays for the full sweep. Companies with a zero/negative trailing P/E are excluded, not clamped, since a negative P/E doesn't imply a meaningful earnings yield at all. Tested locally the same way as this session's other page (synthetic 503-company dataset, Playwright, dark/light/mobile with the `localStorage.theme` fix, local stub API + vendored Chart.js) — no bugs found.
* **Earnings Growth vs. Price Performance Divergence** (2026-09-14, branch `claude/bold-clarke-oi26nk`, same branch/PR as the two pages above) — `/earnings-growth-divergence.html`. Trailing year-over-year quarterly EPS growth (Alpha Vantage `EARNINGS`, latest quarter vs. the same quarter 4 quarters back, to avoid seasonal noise) vs. 3-month price performance relative to SPY. Deliberately does **not** run a second full-universe price sweep: it reads this same night's Relative Strength Leaders/Laggards `latest.json` blob directly (`getRelativeStrengthStore`) for the price half of the comparison — a real, documented cross-page dependency (scheduled at 14:00 UTC — moved from its original 13:00 after both Relative Strength Leaders/Laggards and Equity Risk Premium by Sector were themselves rescheduled to resolve unrelated collisions during this PR's merge into `main`; runs after Relative Strength Leaders/Laggards, now at 13:10, and Equity Risk Premium by Sector, now at 13:35, specifically so that data is fresh same-day), with a graceful EPS-growth-only fallback (and an on-page warning banner, tested directly) if that blob isn't populated yet. Sector-paired bar chart, an EPS-growth-vs-price-return scatter with Pearson+Spearman, and two "divergence score" leaderboards (rank-by-EPS-growth minus rank-by-price-return, in both directions) — Growth Ahead of Price / Price Ahead of Growth. Companies with a loss in the year-ago quarter are excluded from the growth figure (not shown as a distorted or infinite %), same convention as Equity Risk Premium's P/E exclusion. Name/sector come from Sector Beeswarm's `meta.json`, same as every other full-universe job on this site. Tested locally the same way as this session's other two pages, including a dedicated "relative-strength blob not yet populated" scenario to confirm the fallback path and warning banner both work — no bugs found.
* This was a three-page night (all in one PR/branch) rather than the usual one-or-two: Relative Strength Leaders/Laggards and Equity Risk Premium by Sector are independent full-universe sweeps: Earnings Growth vs. Price Performance Divergence deliberately builds on the first rather than adding a fourth ~503-call sweep, so the cost profile for the night is roughly two-and-a-half full sweeps, not three. Also worth noting for future runs: this session's testing caught a real methodology gap in how the *previous* two sessions' `light-desktop` Playwright variants were set up — this site drives its light/dark theme off a `localStorage` flag read at page load, not `prefers-color-scheme`, so a bare Playwright `colorScheme: "light"` context option (as used in the light-mode tests described in the Relative Strength/Small Cap/Sector Rotation/Share Count Trends entries above) never actually rendered the light theme — the "light-desktop" screenshots in those runs were dark-theme renders. No visual bug was found once corrected (this session re-verified both of this session's own new pages properly, via `page.addInitScript` setting `localStorage.theme = "light"` before navigation), but any future run relying on a *prior* run's "tested in light mode" claim for one of those four specific pages should treat it as unverified and spot-check directly rather than assuming it was actually covered.
* **Small-Cap Liquidity & Volume Trends** (2026-09-15, `nightly/2026-09-15-ipo-pipeline`) — `/small-cap-liquidity.html`. Amihud (2002) illiquidity ratio and Corwin-Schultz (2012) high-low bid-ask spread estimates for the smallest vs. largest quintile of S&P 500 constituents by market cap (a market-cap-within-the-S&P-500 proxy — explicitly flagged as not true Russell 2000 small caps, no free full small-cap-index constituent list was available), plus a Pearson+Spearman regression testing whether the Small/Mega liquidity gap widens in the same months small-caps underperform large-caps (reusing `/api/smallcap-largecap`'s existing monthly-return bucketing logic client-side). Backend: new weekly `scheduled-smallcap-liquidity-background.js` (Saturday 14:20 UTC — moved from its original 13:35 UTC, which the PR chose to clear only the jobs it could see at the time; that slot turned out to collide with `scheduled-equity-risk-premium-background` once every concurrently-built PR's own schedule fixes were applied — see that function's own schedule comment. ~200 sequential `TIME_SERIES_DAILY_ADJUSTED` calls, reuses Sector Beeswarm's `meta.json` for cohort membership/market cap — no new OVERVIEW sweep) → Netlify Blobs (`smallcap-liquidity` store) → `smallcap-liquidity.js`. Tested with Playwright (dark/light/mobile, real Chart.js served locally since this sandbox blocks jsdelivr) against a synthetic 200-ticker/504-day dataset — one real bug found and fixed pre-commit: the regression method table wasn't overflow-contained and pushed 371px of horizontal overflow at 390px width.
* **IPO Pipeline & Aftermarket Tracker** (2026-09-15, `nightly/2026-09-15-ipo-pipeline`) — `/ipo-pipeline.html`. Companion to (not a replacement for) `/ipo-activity.html`: tracks Alpha Vantage's forward-looking `IPO_CALENDAR` (~3 months out, CSV format), classifies each row Operating Company vs. Fund/Trust/SPAC by name pattern, and — since Ritter's historical dataset behind `/ipo-activity.html` has no per-IPO ticker to join price history against (confirmed dead end, see the Short Interest/IPO note below and PR #13's own investigation) — builds an honest **forward-only** aftermarket track record: once a tracked operating-company IPO's listing date passes, follows its day-1/5/10/20 return vs. SPY going forward from whenever this page first started tracking it. Explicitly does NOT attempt to annualize or forecast off the thin ~3-month calendar window (methodology calls this out as "a floor, not a forecast," with Ritter's trailing-5-year annual counts shown only as separate background context, never combined into one number). Backend: new weekly `scheduled-ipo-pipeline-background.js` (Saturday 13:20 UTC — trivial call volume: 1 `IPO_CALENDAR` pull + a handful of `TIME_SERIES_DAILY_ADJUSTED` compact pulls for roster entries due for a check) → Netlify Blobs (`ipo-pipeline` store) → `ipo-pipeline.js`. Cold-start page (pipeline-size trend and aftermarket-tracking table both start sparse/empty and build up over subsequent weekly runs) with graceful empty states for both. Tested with Playwright against synthetic mocks covering full/mixed, cold-start, and all-empty roster states — one real bug found and fixed pre-commit: the aftermarket table's "complete entries first" sort compared a row object to a string instead of `row.status`, silently degrading to a pure date sort.
* **Status as of 2026-09-16: all four other Equities PRs this session flagged as open/unmerged — #4, #12, #13, #14 — plus this run's own PR (#15) have since been rebased onto current `main` and merged, one at a time, resolving several real Saturday-cron collisions along the way (each affected function's own schedule comment documents the collision and the fix).** All pages from every PR are now live; no further merge-order action needed. This note is kept as the historical record.
* **Spin-Off Performance Tracker** (2026-09-15, `nightly/2026-09-15-spinoff-performance`) — `/spin-off-performance.html`. Tests the "spin-off effect" (spincos and their remaining parents tend to outperform for a stretch after separation, one common explanation being forced index-fund selling of the new, unfamiliar spinco) across 19 hand-compiled spinoff events since 2015 (IBM/Kyndryl, GE/GEHC, GE/GEV, DowDuPont/Dow, DowDuPont/Corteva, three Danaher spinoffs, 3M/Solventum, JNJ/Kenvue, Kellanova/WK Kellogg, Honeywell/Resideo, the HP split, eBay/PayPal, Xerox/Conduent, Merck/Organon, Pfizer/Viatris, and UTC's Otis/Carrier spinoffs — the latter two with no parent leg since UTC merged into Raytheon the same day). Event dates/tickers are hand-compiled from public record (same "curated metadata, real market data" split as `/expectations-vs-reality`); every return number is a real Alpha Vantage `TIME_SERIES_DAILY_ADJUSTED` pull (adjusted close) against SPY as the benchmark — nothing about the price data is fabricated. An event-time-aligned average cumulative excess-return curve (every leg indexed to its own separation date, N shrinking transparently at longer horizons), a one-sample t-test plus an exact two-sided sign test on excess return at the 1-year mark (run separately for spinco legs, n≈19, and parent legs, n≈17, matching /factor-analysis's two-method-check spirit and its honesty about small-sample limits), a spinco-vs-parent excess-return scatter with Pearson+Spearman regression, best/worst leaderboards, a sector breakdown, and a full sortable per-leg table. Backend: weekly `scheduled-spinoff-background.js` (Saturday 11:02 UTC — moved from its original 11:10 UTC after a same-minute collision with `scheduled-surprise-background`, from a concurrently-built PR, was caught and fixed while merging this PR into `main`; runs after `scheduled-share-count-background`, finishing ~11:00) — only ~33 unique tickers (19 spincos + 13 unique parent tickers, several reused across more than one event, + SPY), so minimal rate-limit risk — → Netlify Blobs (`spinoff-performance` store) → `spinoff-performance.js`. Any ticker/leg whose price history doesn't reach back to its event's anchor date is dropped gracefully (logged in `skippedLegs`) rather than fabricated or blocking the page; a few events carry footnoted nuances (Viatris is a Reverse-Morris-Trust spin-merger not a pure spinoff, Kenvue is anchored to the exchange-offer completion date not the earlier IPO carve-out, Kellanova/WK Kellogg's parent ticker kept trading under a new name). Tested locally by running the actual production background-job code against a mocked Alpha Vantage fetch (synthetic-but-realistic GBM price paths, real event dates/tickers, a deliberate strong-drift boost on a few spincos and a drag on a couple others, plus one ticker forced to fail every attempt) and a mocked blob store, served through a local stub API, driven with Playwright in dark/light desktop and dark/light mobile — caught and fixed one real bug pre-merge: the two method-summary tables (t-test/sign-test and the scatter's regression stats) sat directly in a `.chart-box` rather than a `.table-box`, so the site-wide `table{min-width:600px}` rule forced the whole page wider than a phone viewport; fixed by giving those two containers their own `overflow-x:auto`.
* **Margin & Leverage Cycle** (2026-09-15, `nightly/2026-09-15-margin-leverage`) — `/margin-leverage.html`. A fresh, off-list idea (per this brief's explicit permission to propose beyond the listed backlog) tracking S&P 500 corporate profitability and balance-sheet leverage through the recent Fed rate cycle — no existing page touches income-statement margins or balance-sheet leverage ratios. Sector-median quarterly trend (toggle: gross/operating/net margin) and sector-median net-debt/trailing-twelve-month-EBITDA trend across the 11 SPDR sectors; the core "why this matters" test aggregates to one point per calendar quarter (the cross-company median QoQ change in operating margin, and separately in net-debt/EBITDA) rather than a ~500-way pseudo-replicated company panel, then compares those by Fed-funds regime (Hiking/Holding/Cutting, using the identical trailing-3-month-change classification already built for `/small-cap-vs-large-cap.html`) and regresses each against the quarter's Fed-funds change (Pearson+Spearman, matching `/factor-analysis`'s two-method convention) — honestly reported as a small-sample test (only a few dozen quarters of history) given how sticky/slow-moving margins and leverage are; in this run's synthetic-but-realistic test fixture the planted signal showed cleanly (median margin change averaged about -0.40ppt/quarter in Hiking regimes vs. +0.20ppt/quarter in Cutting regimes), but that's a property of the test fixture, not a claim about the real production data, which will show whatever it actually shows once the real weekly sweep runs. Also a margin-vs-leverage cross-company scatter (most recent quarter, Pearson+Spearman), sector-relative leaderboards (widest/thinnest margin, most/least levered vs. each company's own sector median, not raw cross-sector), and a full sortable per-company table with a trailing-4-quarter margin trend arrow. Data: Alpha Vantage `INCOME_STATEMENT` + `BALANCE_SHEET` (quarterly, full S&P 500) + `FEDERAL_FUNDS_RATE` (monthly). Net debt prefers Alpha Vantage's combined `shortLongTermDebtTotal` field, falling back to summing `shortTermDebt`/`longTermDebt` when that's `"None"`; EBITDA prefers Alpha Vantage's direct `ebitda` field, falling back to `operatingIncome + depreciationAndAmortization` (same INCOME_STATEMENT report) when that's `"None"` — both fallback paths, and the case where even the fallback fields are missing (ratio left `null` rather than fabricated), are exercised by the test fixture. Backend: weekly `scheduled-margin-leverage-background.js` (Saturday 14:35 UTC — moved from its original 11:20 UTC, which directly overlapped `scheduled-shareholder-yield-background`'s own 11:25-~11:34 sweep, a separate, concurrently-built PR's job unaware of this one; now runs after `scheduled-smallcap-liquidity-background` at 14:20, the last job in the Saturday block as of this fix — see this function's own schedule comment in `netlify.toml` for the full account) — a two-statement sweep (~1006 Alpha Vantage calls) paced at 750ms between calls (tighter than the usual ~800-1050ms elsewhere in this codebase, specifically because doubling the calls-per-company nearly doubles total sweep time inside a Background Function's ~15-minute ceiling) plus a retry pass — reusing Sector Beeswarm's `meta.json` for company name/sector rather than a third full-index metadata sweep. Tested by running the actual production background-job handler against a mocked `fetch` (18 real-symbol tickers across 5 sectors with realistic synthetic quarterly financials built on a real-shaped 2019-2026 Fed-funds path, a deliberately-planted margin-compression/leverage-rise signal during the synthetic 2022-23 hiking stretch and partial recovery during 2024-26 cutting, one ticker whose every fetch attempt fails, and three tickers individually exercising the `shortLongTermDebtTotal`-missing, `ebitda`-missing, and both-EBITDA-inputs-missing fallback paths) and a mocked Netlify Blobs store, served through a local stub API (reusing this session's Chart.js/zoom-plugin vendor files and stub-server pattern already set up earlier this run for the Spin-Off Performance Tracker test), driven with Playwright in dark/light desktop and dark/light mobile. Caught and fixed one real layout bug pre-merge: the two-column `.chart-grid` used for the regime-comparison and rate-regression charts didn't protect its grid items from CSS Grid's default `min-width:auto` "blowout" behavior, so a method-summary table's intrinsic width forced the whole page wider than its container (worse on mobile, but present at desktop width too) — fixed with an explicit `min-width:0` on `.chart-grid > .chart-box` (and on `.method-table` itself, which was also inheriting the site-wide `table{min-width:600px}` rule meant for the main multi-column data tables). Opened in parallel with PR #16, Spin-Off Performance Tracker (`nightly/2026-09-15-spinoff-performance`) — both have since been rebased and merged into `main`, one at a time, resolving the real cron collision noted above along the way.
* **Analyst Ratings & Price Targets** (2026-09-17, `nightly/2026-09-17-analyst-price-targets`) — `/analyst-price-targets.html`. A fresh, off-list idea (per this brief's explicit permission to propose beyond the listed backlog): turns two independent sell-side signals Alpha Vantage's `COMPANY_OVERVIEW` already carries — the `AnalystRatingStrongBuy/Buy/Hold/Sell/StrongSell` breakdown and `AnalystTargetPrice` — into a weighted 1–5 consensus score (1=Strong Buy, the standard Zacks-style convention) and a price-target-implied upside, across the full S&P 500. Distinct from `/analyst-estimate-dispersion.html` (that page is about disagreement over the *EPS estimate*; this one is about buy/sell recommendations and price targets, a different pair of fields). Deliberately built as a single-snapshot, cross-sectional page rather than an accumulating one: on 2026-09-16 (the prior session) the site owner switched most of the recently-built analytical pages' scheduled functions from a recurring weekly cron to no schedule at all ("one-time snapshot... meant to read as the research as it stood on this date" — see `scheduled-surprise-background`'s comment in `netlify.toml`), including `scheduled-relative-strength-background` itself; building yet another page whose only real test needs several weeks of accumulated history to stop being cold-started would work against that decision. So both statistical tests here are single-pull cross-sections: (1) consensus score vs. implied upside — do the two independently-reported signals actually agree with each other (Pearson+Spearman, expect a negative slope)? — and (2) implied upside vs. trailing 3-month relative return, reusing `/relative-strength-leaders.html`'s own already-computed `rel3M` figure (no second price sweep) — do analysts extrapolate the recent trend into their targets, or not? Sector consensus/upside bar charts, an implied-upside distribution histogram, Most/Least Bullish and Highest/Lowest Upside leaderboards (min. 5 covering analysts for consensus, matching Analyst Estimate Dispersion's threshold), and a full sortable per-stock table. Current price is deliberately proxied by `50DayMovingAverage` rather than a live quote (`OVERVIEW` has no current-price field, and a separate `GLOBAL_QUOTE`/`TIME_SERIES` sweep would double this page's Alpha Vantage cost) — the page's methodology section says so explicitly, the same "derive it from what a sweep already returns" trade-off `/pe-divergence.html` and `/equity-risk-premium.html` make. Backend: `scheduled-analyst-price-targets-background.js`, ~503 sequential `COMPANY_OVERVIEW` calls at 1050ms spacing plus a retry pass — added to `netlify.toml` with **no schedule from the start**, following the same one-time-snapshot convention now used by its siblings; needs a manual Netlify dashboard "Run now" after this deploys to populate the initial blob (the page shows a clean "not yet populated" state until then — verified directly in testing). Reads name/sector off each stock's own `OVERVIEW` response rather than a shared metadata blob (same reasoning as `/equity-risk-premium.html`, which already pays for the sweep). While wiring up the nav for this page, found and fixed real pre-existing staleness the CLAUDE.md nav gotcha warns about: several pages' copies of the Equities nav dropdown (e.g. `margin-leverage.html`'s own nav, ironically the newest page at the time) were missing a dozen-plus entries added by other same-night sessions, and the homepage's Research Areas grid was separately missing both `sector-beeswarm.html` and `spin-off-performance.html`. Reconciled all ~40 pages' Equities dropdown to one canonical list (matching `index.html`'s, which was the most complete) plus this page, and fixed the homepage grid gaps, while adding this run's own entry — a mechanical, script-driven pass (diffed and spot-checked before committing), not a manual per-file edit. Tested with Playwright (dark/light desktop + mobile, real Chart.js/zoom-plugin vendored locally via `npm pack` since this sandbox blocks the jsdelivr CDN) against a synthetic 150-ticker dataset, plus dedicated "no relative-strength data available" and "not yet populated" fallback scenarios — one real bug found and fixed pre-commit: a long `AnalystRatingStrongBuy/Buy/Hold/Sell/StrongSell` code span with `white-space:nowrap` overflowed a 390px mobile viewport (split into separate short `<code>` tags), plus the same `.table-grid`/`.method-table` CSS Grid "blowout" `min-width:0` fix earlier sessions already had to apply to `.chart-grid` on other pages, applied here from the start.
* **Institutional & Insider Ownership** (2026-09-17, `nightly/2026-09-17-analyst-price-targets`, same branch/PR as Analyst Ratings & Price Targets above) — `/institutional-ownership.html`. A fresh, off-list idea, and this session's cheapest page to date: it makes **zero** Alpha Vantage calls of its own. `COMPANY_OVERVIEW`'s `PercentInstitutions`/`PercentInsiders`/`MarketCapitalization` fields ride along on Analyst Ratings & Price Targets' own same-night sweep (that page's charts don't use them, but the response already carries them), so this page's background is just the `analyst-price-targets-background.js` edit that adds those three fields to the stored `companies` array — the same "the job that already pays for the full sweep gets read by a same-night sibling" reuse as `/shareholder-yield.html` reading `/share-count-trends.html`'s output, just skipping the extra backend/blob-store hop entirely: this page's frontend fetches `/api/analyst-price-targets` directly and does all sector aggregation, the histogram, and both regressions client-side. Sector institutional/insider ownership bar charts, an institutional-ownership distribution histogram, and two real cross-sectional tests: (1) institutional ownership % vs. analyst coverage count (does more institutional money coincide with more analyst attention? Pearson+Spearman), and (2) log₁₀(market cap) vs. insider ownership % (the classic stylized fact that insiders own a bigger percentage stake in smaller companies — tests whether it holds even within the S&P 500's already-large-cap-only universe). Highest/Lowest leaderboards for both ownership measures and a full sortable table. Single-snapshot, no accumulating history, matching its sibling page's schedule (or rather lack of one). Tested the same way as Analyst Ratings & Price Targets (Playwright, dark/light desktop + mobile, vendored Chart.js, synthetic dataset with planted institutional-coverage and insider-vs-size relationships, plus a "not yet populated" fallback check) — no new bugs, since the CSS grid/table fixes from that page's own testing were applied here from the start.

Already attempted, skipped
(Update at the end of every run with why, so future runs don't re-try the same dead end)

* **Earnings Surprise History (beat/miss rates by sector)** and **Dividend Growth Screener** — both built independently in *this* session (Alpha Vantage `EARNINGS` and `DIVIDENDS` respectively), tested, and initially shipped as `nightly/2026-09-13-earnings-surprise` / part of `nightly/2026-09-13-dividend-growth` — then removed and dropped from the stack once it turned out a separate, concurrently-running session had already built both of these same two ideas first, unmerged, in PR #4 (`claude/sharp-hopper-t1gvy2`, created 2026-09-13 10:14 UTC — well before this session started, but never merged to `main`, so this session's `ROUTINE_BRIEF.md` read at start-of-run had no way to know). Real duplicate work, not a design flaw in either implementation. **Status as of 2026-09-16: PR #4 has been rebased onto current `main` and merged.** Both pages are now live (`/earnings-surprise.html`, `/dividend-growth-screener.html` — see the "Already built" entries above) — no further action needed, and this note is kept only as the historical record of the duplicate-work incident.
* **New Highs/New Lows Ratio** — skipped, not a data gap. `/market-breadth.html` already has a "New Highs − New Lows" stat card and a dedicated "52-week new highs vs. new lows" chart; a separate page would be a near-duplicate.
* **Equity Factor Performance (value/growth/momentum/quality/low-vol)** — skipped, not a data gap. `/factor-analysis.html` already covers Market/Size(SMB)/Value(HML)/Momentum via the Ken French Data Library back to 1926; a separate page would mostly re-plot the same series. (Quality and low-vol specifically aren't covered — could be a future addition to that existing page rather than a new one, if picked up later.)
* **IPO Aftermarket Performance** — a true historical version (join Ritter's `/ipo-activity.html` data against price history to compute actual post-IPO returns) remains not buildable: Ritter's dataset is aggregate year-level stats only, no individual IPO ticker list, confirmed by PR #13's investigation and re-confirmed this session. **Partially superseded**: this session's new IPO Pipeline & Aftermarket Tracker (`/ipo-pipeline.html`, see "Already built" above) covers the forward-looking version of this idea instead — don't rebuild a historical-backfill attempt, it's a confirmed dead end; if picked up again, the angle worth exploring is a different free historical source with a real per-IPO ticker list (none found in two separate sessions' searches so far).
* **Short Interest Tracker** (2026-09-15) — investigated and skipped, not built. FINRA's old anonymous `equityShortInterest` data endpoint was deprecated in April 2021; its replacement now lives behind FINRA's API Console and requires a registered FINRA API Console account with OAuth client credentials (confirmed via web research this run). That registration isn't configured in this environment and can't be self-served headlessly — it needs a human to sign up for a FINRA API Console account, create an OAuth client, and add the resulting credentials as a Netlify env var before this idea is buildable. **Manual decision needed**: someone with FINRA API Console access has to do that registration; until then, leave this skipped rather than re-investigating the same dead end.
* **Buyback Announcements** (dollar-value announced-buyback-program tracking, distinct from the share-count-based buyback signal already covered by `/share-count-trends.html`) — no Alpha Vantage endpoint, and no reliable free public source with a maintained ticker-level dataset was identified in the session that investigated this (search was limited by that session's sandbox network-egress restriction, which blocked most non-Alpha-Vantage domains outright — only `WebSearch`, not `WebFetch` or direct `curl`, could even query most candidate sources). Not written off entirely, just not attempted — worth a real look in a less network-restricted session. (**Spin-off Performance Tracker**, investigated alongside this at the time, has since been built — see the "Already built" entry above; it turned out real Alpha Vantage price data plus a small hand-compiled event list was sufficient, no separate spin-off-specific data source was actually needed.)
* **Buyback Announcements — re-investigated 2026-09-17**, this time with a working `WebSearch` (no egress restriction hit this run). Still no free, maintained, ticker-level dataset: the only specialized option found (Apify's "Stock Buyback Announcement Tracker") is a paid scraper product, not a free API, and MarketBeat/TipRanks' buyback pages are web pages meant for humans, not a documented free feed. Treat this as a confirmed dead end now (not just an unattempted one due to network limits) unless a future run finds a genuinely free, ticker-level source this search missed — don't re-run the same generic search again.
